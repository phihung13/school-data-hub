# Sinh ra bởi tools/check-html.mjs để node --check kiểm cú pháp JS nhúng
tools/.tmp-inline.js

node_modules/
dist/
build/
.next/
coverage/

# Secret không bao giờ vào repo (§4)
.env
.env.*
!.env.example
*.pem
*.key

# Tệp nhật ký báo động do tools/alert/kenh/tep-nhat-ky.mjs ghi ra (0051).
# Là ĐẦU RA vận hành của máy đang chạy, không phải mã nguồn — và nó mang tên job,
# tên luật, tình trạng máy chủ, nên càng không phải thứ đẩy lên kho chung.
var/

# Log của tools/start-local.sh (tiến trình nền) — không thuộc mã nguồn.
.hub.log
.hub.err.log
.cloudflared.log

# Bản dựng thật — tách khỏi .next của chế độ lập trình viên (next.config.mjs).
apps/hub/.next-prod
