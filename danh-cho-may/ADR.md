---
ban-doi-ung: none
sync-version: 6
---

# Sổ ADR — Architecture Decision Records

Mẫu đề xuất mới: `templates/adr.md`. Trạng thái: Duyệt / Duyệt có điều kiện / Đề xuất.

| ADR | Quyết định | Bối cảnh ngắn | Trạng thái | Xét lại khi |
|---|---|---|---|---|
| 001 | Monolith Next.js + tRPC + Supabase, một Postgres | 2 dev, stack đã thạo; 11 nguồn cần một sự thật | Duyệt | >10k user hoặc >1M req/ngày |
| 002 | Dữ liệu cảm xúc lưu như dữ liệu thường: không mã hóa, không schema riêng; mood trong `evidence`, `counselor_notes` trong `care`, phân quyền ma trận chung | Chủ tịch 19/07, tái khẳng định 23/07/2026: không phức tạp hóa | Duyệt (final) — giữ 2 lời hứa công khai (không xếp loại bằng cảm xúc §5; xóa chi tiết 12 tháng); hành động kèm còn lại chỉ là văn bản: tu chính Hiến chương điều 3 | — (đã tái xác nhận, không chờ phiên Hội đồng) |
| 003 | Thiết bị = điện thoại cá nhân; PWA, chưa lên store; sau ổn định lên cả CH Play + App Store qua Capacitor | Chủ tịch 19/07, ghi đè BRD "THCS 100% laptop" | Duyệt (kèm errata BRD) | sau gate T12/2026 |
| 004 | Cờ cảnh báo là if-then đọc config, không ML | minh bạch, kiểm chứng được, chưa có dữ liệu huấn luyện | Duyệt | sau ≥2 năm dữ liệu |
| 005 | Bỏ Google Classroom; Moodle cho khóa online + ngân hàng đề | BRD Final 15/07 | Duyệt | — |
| 006 | Backup 3-2-1: PITR + pg_dump đêm (role `backup_reader` read-only, GH Actions) → R2 mã hóa age + ổ cứng trường hằng tuần | chống mất dữ liệu cấp vendor; trade-off: 1 secret read-only trong GH | Duyệt | khi trường có máy chủ nội bộ |
| 007 | Điểm danh = IP trường (server kiểm) + khung giờ + GVCN xác nhận lệch; QR động 30s là nâng cấp ngủ; bản sync muộn = `queued_late`, không tự tính chuyên cần | chống gian lận không xâm phạm (không GPS/camera) | Duyệt | nếu gian lận thực tế vượt mức GVCN xử được |
| 008 | Break-glass vùng lõi: 1 dev vắng >48h + sự cố cao → merge với 1 approve + security-agent review + hậu kiểm 72h, log và báo Chủ tịch | bus factor 2 dev | Duyệt | khi đội ≥3 dev |
| 009 | Tách dữ liệu y tế khỏi vùng mở: `evidence.meal_sleep_logs` (mở) + `care.health_logs` (lõi, RLS riêng) | y tế trẻ em là dữ liệu nhạy cảm Luật 91/2025 | Duyệt | — |
| 010 | Flag engine chỉ đọc qua signal views `care.v_signal_*`; bỏ Supabase Realtime (buồng lái fetch khi mở) | chống coupling ngầm schema; bỏ dependency thừa | Duyệt | nếu xuất hiện nhu cầu realtime thật |
| 011 | **Hub là nền tảng Super App + Mini App.** Core Data Model (users, students, teachers, parents, schools, classes, roles, permissions) là Single Source of Truth trong `core`; Mini App chỉ giữ dữ liệu nghiệp vụ, FK về core, không bản sao; một Postgres chia schema theo domain (attendance, care, health, evidence, tutor; đặt chỗ finance, social, ai); Mini App lớn tách được thành service riêng mà không đổi core model | Chủ tịch chốt: Hub không phải một ứng dụng nghiệp vụ mà là nền tảng thống nhất dữ liệu + trải nghiệm | Duyệt | khi một Mini App cần tách service |
| 012 | **Auth indirection + cách ly hạ tầng.** Supabase Auth UID → `core.users` → roles → permissions → Mini App; cấm đọc `auth.users` trực tiếp; mọi truy cập DB/Auth/Storage qua adapter tập trung — Supabase chỉ là nơi host, đổi sang Neon/self-host/Cloud SQL không viết lại nghiệp vụ | tránh khóa chặt vào nhà cung cấp; nghiệp vụ sống lâu hơn hạ tầng | Duyệt | khi đổi nhà cung cấp hạ tầng |
| 013 | **MD-07: Super App là PWA, không Flutter.** Giữ nguyên TypeScript/Next.js/tRPC; loại cả phương án Flutter thuần (Fastify + Zod→OpenAPI→Dart) và vỏ Flutter + Mini App webview | Chủ tịch chốt: không chuyển sang Flutter | Duyệt | nếu phát sinh nhu cầu native sâu (offline mạnh, sinh trắc học) — mở ADR mới, không quay lại phương án Flutter cũ |
| 014 | **Hub là Identity Provider cho hệ ngoài qua OIDC bridge.** `node-oidc-provider` mount trong cùng deployable (không service riêng); Moodle là RP đầu tiên; khớp tài khoản qua `core.id_mappings`; bridge chỉ cấp định danh, không cấp quyền gọi API Hub | Yêu cầu 24/07/2026: một định danh duy nhất — đăng nhập Hub rồi vào thẳng hệ ngoài | Duyệt | khi số RP ngoài đủ lớn cần bảng client registry + màn hình quản trị thay vì config tĩnh |
| 015 | **Chuẩn Embedded Mini App — 3 tier tin cậy (Native/Embedded/Linked).** Embed Bridge nhúng iframe qua OIDC (ADR-014) + postMessage/context token; Embed API scoped thay service_role; cấm app ngoài (kể cả build bằng Base44, Google AI Studio, Lovable) lưu định danh học sinh thật, chỉ external reference qua `core.id_mappings`; nút quay lại Hub nằm ngoài iframe | Yêu cầu 24/07/2026: chuẩn hóa việc nhúng app tự code hoặc vibe-code vào Shell, không đăng nhập lại, không rò dữ liệu ra khỏi tầm kiểm soát | Duyệt | khi có ≥5 app Tier 2 cần bảng client registry + màn hình quản trị thay vì config file |
| 016 | **Vòng đời định danh khép kín.** Đăng xuất chung (`end_session_endpoint` + back-channel logout); token ≤15 phút và mỗi lần refresh kiểm `core.users.status` ⇒ khóa tài khoản là cắt mọi hệ trong một chu kỳ; scope `hub_profile` trả `hub_role`/`hub_school`/`hub_classes` để Moodle tự xếp lớp; tách `core.identity_links` (đăng nhập, mọi loại tài khoản) khỏi `core.id_mappings` (dữ liệu, chỉ học sinh), UNIQUE cả hai chiều. Kèm theo: flag engine bỏ qua rule có nguồn hết tươi (`ops.source_freshness`) và không leo thang cờ `origin='backfill'`; thống nhất một tên `ops.job_runs` | Rà soát 27/07/2026: ADR-014 mới làm chiều mở, thiếu chiều đóng — phòng máy dùng chung giữ phiên người trước, tài khoản đã khóa vẫn vào được Moodle; `id_mappings` không có cột `user_id` nên giáo viên không map được | Duyệt | khi có RP thứ 3–4 cần bảng client registry |
| 017 | **Ba rổ dữ liệu + ổ cắm chuẩn cho app ngoài.** Xanh/Vàng/Đỏ khai trước Tier (Đỏ = `care`/`counselor_notes`/`health`/mood — cấm tuyệt đối, CI chặn); alias do Hub sinh, mỗi app một dải riêng; ghi qua `staging` chạy ngay theo sự kiện; quyền đi theo người dùng chứ không theo app, **không cấp API key cho app không có backend riêng**; `postMessage` kiểm origin + schema + timeout thay cho token trong query string; Manifest có hạn rà lại 6 tháng; kèm app mẫu + trang hướng dẫn cho vibe team | Chủ tịch 27/07/2026: app vibe-code (fitness, căn tin, bảng tin) là hướng lâu dài và sẽ có nhiều — cần chuẩn hóa rõ thay vì mỗi app một kiểu; ADR-015 chưa trả lời được "app ghi dưới danh nghĩa ai" trong khi RLS xây theo danh tính người | Duyệt | khi có ≥5 app Tier 2 (cùng mốc DEBT #8) |
| 018 | **App runtime tự host trên VPS, thay Vercel.** Next.js + tRPC chạy trên VPS riêng, PM2 + Nginx + Let's Encrypt, CI tự deploy + rollback một lệnh; **CSDL/Auth/Storage giữ nguyên ở Supabase** — không đổi ADR-011/012, chỉ đổi nơi host phần app | 28/07/2026: chi phí nền tảng quản lý sẵn (Vercel) cao hơn nhiều so với thuê máy trần cho cùng tải (3,5 req/s trung bình); đổi lại 2 dev tự viết script deploy/rollback thay vì có sẵn | Duyệt | nếu cần auto-scale ngang thật hoặc 2 dev không kham nổi vận hành VPS — sizing cụ thể xem ADR-019 |
| 019 | **Chốt hạ tầng đơn giản nhất trước go-live (sửa sizing ADR-018 + sửa backup ADR-006).** Một VPS duy nhất **16 vCPU/32 GB RAM** chạy toàn bộ app — không nhiều VPS, không load balancer, không Kubernetes. Supabase Pro lo CSDL/Auth/Storage ở compute tier mặc định (không mua thêm PITR, không thêm compute cao hơn trừ khi k6 fail). Code trên GitHub. Backup còn 2 lớp: backup hằng ngày mặc định Supabase + pg_dump hằng tuần chép thẳng vào ổ cứng trường (bỏ PITR, bỏ lớp kho trung gian R2/B2) | 28/07/2026: ưu tiên tối đa sự đơn giản và ít dịch vụ phải vận hành với 2 dev, chấp nhận VPS over-provision xa so với tải đo được (đổi lấy không phải quay lại nâng cấp/scale trong nhiều năm) và RPO tụt (24h/7 ngày thay vì phút/24h) — hệ quả RPO ghi `DEBT.md` #18 | Duyệt | nếu BGH thấy RPO 7 ngày không chấp nhận được sau khi đo thực tế, nếu VPS chạm ngưỡng tải thật (không chỉ lý thuyết), hoặc khi trường có máy chủ nội bộ ổn định hơn |
| 020 | **`core.class_assignments` là nguồn sự thật DUY NHẤT cho quan hệ "GVCN ↔ lớp"; `core.user_role_scopes(homeroom)` bị hạ cấp thành bản sao có hàng rào.** Bản sao không được đi trước bản gốc (trigger từ chối cấp vai khi chưa có phân công); mọi view ứng dụng đọc suy từ bản gốc; phần lệch còn sót phơi ra ở `ops.v_homeroom_drift` thay vì nằm im | Rà 31/07/2026: hai sổ ghi cùng một sự thật, không sổ nào biết sổ kia, và phải gõ tay cả hai vì chưa có màn hình quản trị. Nguyên tắc chọn: **nguồn sự thật phải là nguồn mà HÀNG RÀO đọc, không phải nguồn mà GIAO DIỆN đọc** — chọn ngược lại là cấp quyền bằng một bảng mà tầng dưới không hề hỏi tới. Kiểu hỏng nguy hiểm nhất không báo lỗi: cô mở lớp mình thấy trống rỗng rồi hiểu thành "cả lớp chưa check-in" | Duyệt (`0023` + `0030`) | khi có màn hình quản trị phân công — lúc đó cân nhắc bỏ hẳn bản sao |
| 021 | **Không xóa cứng `core.users`. Đường chính thức là ẩn danh hóa** (`core.anonymize_user`): xóa dữ liệu định danh, giữ dòng và giữ mọi khóa ngoại lịch sử. Cột trỏ *người thao tác* → `on delete set null`; cột *bằng chứng* (audit, can thiệp, ghi chú tư vấn, y tế) → `NO ACTION` có chủ ý. Trigger chặn `DELETE` với thông điệp chỉ đúng đường thay thế, có cửa thoát hiểm khai báo tường minh cho ca hiếm | Luật 91/2025 cho chủ thể quyền yêu cầu xóa, và giáo viên nghỉ việc / học sinh chuyển trường là việc thường ngày. Trước đó không có đường nào, và `delete` thì hỏng nửa vời (kẹt giữa chừng ở một FK, người vận hành nhận mã lỗi Postgres trần). Lý do giữ bằng chứng: cái luật bảo vệ là **thông tin cá nhân**, còn "ai đã làm gì với con tôi" là thứ cũng do chính luật đó bảo hộ — xóa nó là xóa quyền của phụ huynh | Duyệt (`0033`) | nếu Hội đồng dữ liệu hoặc pháp chế yêu cầu xóa cứng thành mặc định |
| 022 | **Chấp nhận trạng thái trong bộ nhớ tiến trình cho rate limit và cho `oidc-provider`, có điều kiện trả ghi rõ.** Không thêm Redis, không thêm bảng phiên, ở quy mô hiện tại | Hub là modular monolith chạy MỘT deployable (ADR-001/018) nên bộ đếm RAM chính là bộ đếm toàn hệ — thêm một dịch vụ phải vận hành cho 2 dev mà không thêm độ đúng nào. **Điều kiện trả là điều kiện kỹ thuật, không phải mốc thời gian:** ngày Hub chạy >1 instance hoặc dùng rolling deploy. Nguy hiểm nhất khi vi phạm: hạn mức cửa mã mời nhân lên theo số instance (`DEBT.md` #22), và khởi động lại tiến trình là đăng xuất cả trường khỏi mọi hệ ngoài (#27) | Duyệt có điều kiện | ngay khi đổi mô hình triển khai |
| 023 | **E_MOOD = 5 ngày mood xấu LIÊN TIẾP** (chuỗi thật), không phải 5 ngày bất kỳ trong cửa sổ 14 ngày. Cách đếm khai trong `care.thresholds.params.mode` (`streak` mặc định \| `window`), không nằm trong mã | Chủ đầu tư chốt 31/07/2026, kết thúc một mâu thuẫn tồn tại từ đầu: tham số tên là `negative_days_streak` (ngụ ý liên tiếp) còn view thì đếm ngày bất kỳ — hai cách đếm cho ra hai tập học sinh khác nhau, và không ai từng chọn. Giữ được cả hai cách trong bảng để lần sau đổi là một câu UPDATE (§6, mệnh lệnh 7) | Duyệt (`0026`) | sau ≥1 học kỳ dữ liệu thật, xem tỉ lệ cờ đúng/sai |
| 024 | **Mã mời phụ huynh là VÉ DÙNG MỘT LẦN, không phải chứng danh.** Đổi mã lần đầu tạo tài khoản phụ huynh; trong **15 phút** sau trả lại đúng `auth_uid` đó (retry mạng, bấm hai lần — §9); sau đó mã chết. Muốn thêm thiết bị hoặc thêm người thì GVCN **cấp mã mới**, và khi có hạ tầng thật thì phiên về sau đi qua Zalo OAuth chứ không qua mã. Cửa sổ **không gia hạn** theo lần dùng cuối. Kèm cùng migration: `revoked_at`/`revoked_by` (đường thu hồi mã gửi nhầm) và `full_name` (tên phụ huynh do GVCN nhập lúc phát mã) | Rà 31/07/2026: `0013:56-60` trả lại phiên **mỗi lần** mã được nhập, cho tới ngày hết hạn, và ghi lý do là "idempotent §9". Đọc kỹ thì §9 nói về **mutation** (gọi hai lần không được sinh hai tài khoản), không nói một chứng danh đăng nhập phải dùng lại được mãi — hai chuyện khác nhau bị gộp làm một. Hệ quả thật: mã 6 ký tự gửi qua Zalo là tấm vé sống 7–30 ngày, ai cuộn lại tin nhắn cũ trong nhóm lớp hoặc được forward cũng vào xem báo cáo/mood/sức khoẻ của một đứa trẻ cụ thể, và người mất quyền nuôi vẫn vào bằng mã cũ vì không có đường cắt. **Đánh đổi đã chọn:** không lấy phương án "chết ngay tức khắc" vì lời gọi commit ở Postgres TRƯỚC khi cookie phiên về tới máy — rớt mạng đúng khoảnh khắc đó (cổng trường lúc tan học) là phụ huynh mất mã mà chưa từng đăng nhập được, không tự sửa được. 15 phút nuốt đúng ca đó, đổi lại còn một khe: mặt tấn công rút từ 7–30 NGÀY xuống 15 PHÚT KỂ TỪ LÚC chính phụ huynh vừa đăng nhập. Con số 15 phút lấy đúng hướng đã ghi sẵn ở `DEBT.md` #28 và trùng `INVITE_CODE_LOCK_MS` của invite-guard | Duyệt (`0036`) | khi nối Zalo OAuth thật — lúc đó mã mời chỉ còn là bước ghép tài khoản lần đầu, cân nhắc rút cửa sổ về 0; hoặc nếu vận hành ghi nhận nhiều ca mất mã vì rớt mạng, xem lại độ dài cửa sổ bằng số liệu `redeemed_count` |
| 025 | **"Ai được thấy em này" là MỘT câu hỏi; "ai được thấy em này CẢM THẤY GÌ" là câu hỏi KHÁC — mỗi câu có hàm phạm vi riêng, không dùng chung.** Từ 31/07/2026, mọi cột/bảng mang tiếng nói bên trong của một đứa trẻ **không được** đi theo `core.can_see_student()` (hợp của sáu nhánh, có `is_my_child` và `principal_of`) mà phải khai phạm vi riêng, có tên, và có pgTAP khẳng định cả chiều từ chối. Ba lần siết trong cùng một ngày: `0035` `care.counselor_notes` → **tác giả + tâm lý cụm** (GVCN mất quyền đọc nguyên văn buổi tư vấn, KHÔNG mất cờ/hồ sơ chăm sóc); `0037` `attendance.help_requests` → `core.is_me() OR core.can_see_care()`; `0038` cột `mood` của `attendance.checkins` → hàm mới **`core.can_read_mood()`** = GVCN + tâm lý cụm, thi hành bằng **GRANT theo CỘT** + view chủ-quyền `attendance.checkins_care`. Hệ quả nghiệp vụ chủ đầu tư đã chốt: **phụ huynh và hiệu trưởng không xem mood từng ngày**, vẫn xem điểm danh và báo cáo tổng hợp. Hệ luận cùng họ ở đầu kia của hệ: `0040` mở đường đọc cho `principal`/`board` **chỉ bằng số tổng hợp**, đơn vị nhỏ nhất là một lớp, nhóm dưới 10 em trả NULL + `cohort_too_small` chứ không trả 0 (DESIGN-GUIDELINES §9) | Cùng một lỗi gốc lặp ba lần và cả ba đều **im lặng**: không lỗi, không log, chỉ là một hàng dữ liệu hiện ra đúng nơi đã hứa là sẽ không hiện. `0009:150-176` áp policy cho 16 bảng bằng MỘT vòng lặp cùng điều kiện; ba bảng trên lọt vào đó vì chúng "gắn student_id", trong khi câu hỏi thật về chúng không phải là câu hỏi quản lý. Đo được trước khi vá trên hub_dev: phiên phụ huynh đọc ra nguyên văn lời em viết ở "cần gặp thầy cô" và 7 ngày mood; phiên hiệu trưởng đọc ra 8 ngày mood; phiên GVCN đọc ra nguyên văn ghi chú tư vấn. Cả ba màn hình tương ứng đều đang IN CHỮ hứa điều ngược lại cho chính đứa trẻ đọc lúc nhập — mà **lời hứa in trên màn hình là ràng buộc kỹ thuật**. Trước khi vá, lời hứa được giữ bằng kỷ luật tầng ứng dụng: đúng một câu `select` viết đúng cú pháp trong tính năng sau là lộ lại. **Đánh đổi đã chọn ở `0038`:** GRANT theo cột chứ không phải view che cột — view che cột làm gãy `INSERT … ON CONFLICT` của `checkin.submitMood`, và tệ hơn, `attendance.rollup_mood_trends()` (`0031`) chạy không có ngữ cảnh người dùng sẽ đọc ra toàn NULL rồi `purge_old_emotion_details()` xoá chi tiết ngay sau đó — mất sạch 12 tháng dữ liệu, không một dòng lỗi | Duyệt (`0035` + `0037` + `0038`, hệ luận `0040`) | khi có **đường chuyển tuyến GVCN → tâm lý cụm mà em đồng ý** (lúc đó `help_requests.note` cần phạm vi thứ ba, không được mở bằng cách nới hàm cũ); hoặc khi có yêu cầu cho phụ huynh xem mood — đó phải là quyết định của Hội đồng dữ liệu và một ADR mới, **không phải một dòng policy** |
| 026 | **Nhật ký cảm xúc từng ngày rời khỏi tầm đọc của giáo viên chủ nhiệm — ĐẢO MỘT PHẦN ADR-025.** `core.can_read_mood()` mất nhánh `is_homeroom_of`; phạm vi mới = `core.is_me() ∨ core.in_my_cluster()`, tức chính em và tâm lý cụm. Hai cửa hậu đóng cùng migration: policy `mood_trends_scope` của `attendance.mood_trends` chuyển từ `can_see_student` sang `can_read_mood`, và `attendance.happy_days()` đổi cổng sang `is_me ∨ is_my_child ∨ in_my_cluster` đồng thời **từ chối khoảng hỏi hẹp hơn 5 ngày** (lỗi `22023`). **KHÔNG đụng `core.can_see_care()`** — đó là lý do cô vẫn nhận cờ `care.flags` và vẫn nhận ngay tín hiệu "cần gặp thầy cô". Tâm lý cụm, chính em, phụ huynh, BGH: không đổi. Đi kèm ở tầng màn hình (cùng commit, không được hoãn): bốn màn GVCN đổi nguồn cột điểm danh về `attendance.checkins`, buồng lái chuyển sang đọc cờ từ `care.flags`, và nhãn rút về đúng chừng — nhãn chuẩn chốt tại `DESIGN-GUIDELINES` §9 | Chủ đầu tư chốt 01/08/2026. ADR-025 đặt đúng câu hỏi ("ai được thấy em này" ≠ "ai được thấy em này CẢM THẤY GÌ") nhưng trả lời còn rộng một vai. Lý do đảo: nhật ký cảm xúc từng ngày là lời một đứa trẻ nói với chính mình, không phải dữ kiện quản lý lớp; để nó trong tầm đọc thường ngày của đúng người chấm điểm và xếp loại em là bắt §5 (cấm dùng cảm xúc xếp loại) sống bằng kỷ luật cá nhân thay vì bằng ràng buộc kỹ thuật. **Đo trước khi quyết, không suy luận:** số dòng có mood đọc được qua `attendance.checkins_care` trên hub_dev — cô Lan 75→0 · cô Mai (tâm lý cụm) 358→358 · Minh (chính em) 9→9 · phụ huynh 0→0. Bộ quét cờ chạy trước/sau trên cùng dữ liệu: 11/11 cờ, phân bố y hệt (A_ATTENDANCE 4 · E_MOOD 3 · E_URGENT 4) — vì engine chạy vai `postgres` và đọc mood qua `care.v_signal_emotion` → `attendance.checkins` trực tiếp. Cửa hậu `happy_days` đo được thật: cô Lan hỏi từng ngày một, 25/07→01/08, nhận chuỗi `1/0/1/1/0/0/0/0` — đọc lại nguyên nhật ký "hôm nay em có Vui không", chỉ khác cách gõ. **ĐÁNH ĐỔI, nói thẳng:** (1) cô mất ngữ cảnh — thấy cờ mà không thấy chuỗi ngày dẫn tới cờ, nên chất lượng cuộc trò chuyện đầu tiên với em phụ thuộc việc cô gọi được tâm lý cụm hay không, mà **đường chuyển tuyến đó chưa tồn tại**; (2) tâm lý cụm thành nút thắt — một người cho nhiều lớp và nay là vai duy nhất ngoài chính em, hỏng người này là hỏng cả tuyến; (3) mất khả năng cô tự phát hiện sớm bằng mắt trước khi ngưỡng bật, đổi lại phải tin ngưỡng `care.thresholds` đặt đúng, mà ngưỡng đó chưa qua một học kỳ dữ liệu thật (ADR-023); (4) buồng lái phụ thuộc hoàn toàn vào nhịp quét đêm cho E_MOOD — engine ngủ một đêm là cô không có gì bù, nên dải "Quét đêm qua" từ chỗ tiện lợi trở thành **thiết bị an toàn**. **Khe còn hở, ghi ra chứ không giấu:** người đã qua cổng `happy_days` (phụ huynh, tâm lý cụm) vẫn lấy hiệu hai khoảng lệch nhau một ngày để suy ra một ngày; chặn kín phải ép khoảng về biên tuần cố định, mà làm vậy thì gãy `report.buildGrowthReport` (thứ Hai→thứ Sáu là khoảng lẻ) — `DEBT.md` #38 | Duyệt (`0044`) | sau một học kỳ dữ liệu thật; **hoặc ngay khi có đường chuyển tuyến GVCN ↔ tâm lý cụm mà em đồng ý** — lúc đó câu hỏi đúng không còn là "cô có được đọc không" mà là "em có đồng ý cho cô đọc không", và đó là một phạm vi thứ năm, không phải nới hàm cũ |
| 027 | **[ĐÃ BỊ THAY THẾ MỘT PHẦN — đọc bản 2 ở dòng dưới trước khi tin dòng này]** **Đồng ý của phụ huynh gác TÀI KHOẢN ĐĂNG NHẬP CỦA HỌC SINH, KHÔNG gác SỔ VẬN HÀNH CỦA TRƯỜNG — và kênh "Mình cần gặp thầy cô" KHÔNG BAO GIỜ bị gác.** Bốn quyết định trong một: **(a) Ranh giới.** Chưa có phiếu đồng ý thì `core.users.status` của em là `'pending'` ⇒ không dựng được phiên (`resolveIdentity` trả null, `core.begin_user_context` trả NULL). Vẫn chạy bình thường: GVCN điểm danh cho em (`checkins_insert_by_homeroom`, `source='teacher'`), y tá ghi nhật ký y tế, phụ huynh xem báo cáo. **(b) Kênh kêu cứu.** `attendance.help_requests` được thêm cột `source` (`self`/`staff`) + `created_by` và policy mới `help_requests_insert_by_care` để GVCN/tâm lý cụm ghi hộ — trước `0046` bảng có ĐÚNG MỘT đường ghi là `core.is_me()`, nên khoá tài khoản em là cắt luôn đường em nhờ giúp. **(c) Bản điều khoản đổi thì ai bấm lại — PHƯƠNG ÁN B.** Cột `core.terms_versions.bat_dong_y_lai boolean not null` KHÔNG có DEFAULT: người tạo bản mới buộc phải trả lời. Chỉ bản đánh dấu `true` mới làm phiếu cũ hết hiệu lực (`core.required_terms_version()`). **(d) Hình dạng sổ.** `core.consent_records` là sổ CHỈ THÊM (trigger chặn DELETE và mọi UPDATE trừ đặt `superseded_at` một lần); khoá idempotent §9 là chỉ mục riêng phần `consent_records_current_uq (user_id, student_id) where superseded_at is null`; ba giá trị `granted`/`declined`/`withdrawn`. Nội dung bản điều khoản bất biến sau khi công bố (trigger, không có cửa thoát hiểm) và neo bằng `content_hash` là cột GENERATED | Yêu cầu chủ đầu tư nguyên văn là "chưa bấm thì tài khoản của con chưa bật", và nó có một cái giá đo được mà hiệu trưởng phải biết TRƯỚC khi ký ô điều kiện mở cửa nhóm Pháp lý. Đo trên hub_dev 01/08/2026, không suy đoán: khoá tài khoản học sinh **không** làm mất điểm danh (bảng `attendance.checkins` có hai đường ghi), **nhưng** nó cắt hoàn toàn `attendance.help_requests` vì bảng đó chỉ có `help_requests_insert_self`. Em nào bố mẹ chưa bấm nút thường đúng là em có phụ huynh ít để tâm nhất — tức hệ thống tự động cắt đường kêu cứu của chính đứa trẻ cần nó nhất, bằng một thao tác hành chính của người lớn. Đó là lý do (b) không phải tuỳ chọn. Với (c): phương án A (mọi lần tăng phiên bản là mọi phụ huynh bấm lại) nghĩa là **sửa một lỗi chính tả = tắt tài khoản của toàn bộ học sinh đang dùng, giữa năm học**, mà mỗi lần thu lại đồng ý mất hàng tuần (hồ sơ đã ghi: suốt tháng 8 thu đồng thuận, đi từng lớp). B bao được A, ngược lại thì không — nên cài B, và rủi ro của B (đánh dấu sai là mất hiệu lực pháp lý mà không ai biết) được bịt bằng việc bỏ DEFAULT. Với (d): khoá `(user_id, student_id, terms_version_id, decision)` như đề xuất ban đầu **gãy** ở chuỗi đồng ý → rút lại → đồng ý lại (lần đồng ý thứ hai đụng dòng cũ, `on conflict do nothing` bỏ qua, trạng thái vẫn là đã-rút — lỗi im lặng); và EXCLUDE thì `ON CONFLICT` không bám được (bẫy đã đo ở `core.enrollments`). **Cái CỐ Ý chưa làm:** `0046` KHÔNG tự chuyển các tài khoản đang `active` về `pending`, vì hôm nay phụ huynh chưa có đường đăng nhập lại (Zalo OAuth chưa nối, mã mời chết sau 15 phút — ADR-024). Khoảng hở đó phơi ra ở `core.v_consent_gap` và có điều kiện trả ở `DEBT.md` #42, không để im lặng | Đề xuất — phần kỹ thuật đã thi hành ở `0046` (40 assertion pgTAP + 7 bài tRPC xanh), phần chờ NGƯỜI KÝ là hai câu: chọn A hay B, và có mở đường phụ huynh tự rút lại ngay trên ứng dụng không. Người quyết: Chủ tịch hoặc hiệu trưởng, không phải dev | ngay khi nối Zalo OAuth (lúc đó mới bật được cổng cho toàn khối — xem `DEBT.md` #42); hoặc khi có bản điều khoản thứ hai, vì đó là lần đầu cột `bat_dong_y_lai` được người thật đánh dấu |
| 027 bản 2 | **Phiếu đồng ý gác VIỆC XỬ LÝ DỮ LIỆU, KHÔNG gác DANH TÍNH của đứa trẻ — đảo vế (a) của bản 1.** `core.users.status` trở lại là công tắc danh tính thuần tuý; `core.sync_student_account_status` bị **bỏ hẳn** và `core.record_consent` không còn ghi vào `core.users` (migration `0047`). Cổng đồng ý dời xuống RLS của `attendance.checkins`: `checkins_insert_self`/`checkins_update_self` = `is_me AND (mood is null OR core.has_student_consent(...))` — gác theo **CỘT**, không theo dòng. Thêm trigger `users_no_pending_downgrade` chặn mọi UPDATE đưa `core.users.status` về `pending` (không có cửa thoát hiểm). **Ranh giới, từng đường một:** **(1) "Mình cần gặp thầy cô" (chính em bấm) — KHÔNG BAO GIỜ khoá**, không bởi phiếu đồng ý và không bởi bất kỳ trạng thái hành chính nào; **(2) ghi tâm trạng hằng ngày — KHOÁ**, vì đó là dữ liệu cảm xúc của trẻ do chính em khai và không có cơ sở pháp lý nào khác ngoài sự đồng ý của người đại diện; **(3) điểm danh (cô ghi VÀ em tự bấm có mặt) — KHÔNG khoá**, nghĩa vụ trông giữ trẻ đứng trên cơ sở pháp lý khác; **(4) đọc Báo cáo Trưởng thành — KHÔNG khoá**, vì Luật 91/2025 đòi đồng ý phải TỰ NGUYỆN: giữ lại thứ phụ huynh vốn có quyền biết về con mình để đổi lấy chữ ký là biến phiếu thành phí vào cửa, và dữ liệu vào báo cáo thì đã bị gác từ đầu nguồn rồi. **Câu hứa với phụ huynh, viết bằng lời họ đọc được:** *"Chưa bấm thì phần mềm chưa ghi tâm trạng của con — và đường con nhờ giúp đỡ thì không bao giờ khoá."* Bản điều khoản **số 2** công bố cùng migration (`bat_dong_y_lai=false`) để nói lại cho đúng; bản 1 bất biến, không sửa. | **Bản 1 có một lỗi CHẶN, đo đầu-cuối trên server thật 01/08/2026, không suy đoán:** phụ huynh bấm rút lại đồng ý → `record_consent` → `sync_student_account_status` đặt `status='pending'` → `core.resolve_user_id_uncached` (`0029`) chỉ trả id khi `status='active'` → `core.current_user_id()` NULL → `core.is_me()` false → **`help_requests_insert_self` câm**. Tức một thao tác hành chính của người lớn cắt đường kêu cứu của một đứa trẻ, và cắt đúng em có phụ huynh ít để tâm nhất. Bản 1 đã LƯỜNG TRƯỚC rủi ro này (mở `help_requests_insert_by_care` cho thầy cô ghi hộ) nhưng bù chưa đủ: ghi hộ là đường của NGƯỜI KHÁC, nó đòi đứa trẻ mở lời trực tiếp với một người lớn trước — mà cái nút trong máy tồn tại chính vì có những đứa trẻ không làm được điều đó. **Chỗ sai gốc là một cột gánh hai khái niệm:** `status` trả lời "người này còn là người dùng của hệ không", phiếu đồng ý trả lời "trường được xử lý dữ liệu nào của đứa trẻ này"; gộp vào một cột thì tắt cái sau là tắt luôn cái trước, mà cái trước là thứ MỌI quyền bám vào. **Câu chủ đầu tư yêu cầu nguyên văn ("em nào chưa có phiếu thì không bật tài khoản") vì thế KHÔNG còn đúng nguyên văn** — nói ra ở đây, trong `02-database.md`, và trong hồ sơ cho người, kèm câu thay thế; câu mới chặt hơn chứ không lỏng hơn, vì câu cũ tắt ĐƯỜNG của đứa trẻ trong khi trường vẫn ghi dữ liệu bình thường. **Đánh đổi nói thẳng:** (i) mức tâm trạng đã thu trước khi rút lại vẫn nằm trong kho — rút lại là "ngừng xử lý từ nay", xoá là quyền riêng phải do người ta yêu cầu (`core.v_mood_khong_phieu` đếm, `DEBT.md` #48); (ii) trường phải chạy chiến dịch xin phiếu thì phần tâm trạng mới bật cho toàn khối — `core.v_consent_gap` gọi tên từng em; (iii) năm bài pgTAP và hai bài vitest ghi mood dưới vai học sinh phải sửa để dựng phiếu trước, vì chúng mã hoá luật cũ. | Đề xuất — phần kỹ thuật đã thi hành ở `0047` (25 assertion pgTAP riêng cho bài khoá chặt + 4 assertion của bản 1 bị lật, 8 ca tRPC xanh). Phần chờ NGƯỜI KÝ: xác nhận ranh giới bốn đường ở trên, đặc biệt đường (4). Người quyết: Chủ tịch hoặc hiệu trưởng, không phải dev | Khi có nút tự rút lại trên ứng dụng (lúc đó (i) thành câu hỏi thật với phụ huynh); hoặc khi có bản điều khoản thứ ba đánh dấu `bat_dong_y_lai` |
