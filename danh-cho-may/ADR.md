---
ban-doi-ung: none
sync-version: 2
---

# Sổ ADR — Architecture Decision Records

Mẫu đề xuất mới: `templates/adr.md`. Trạng thái: Duyệt / Duyệt có điều kiện / Đề xuất.

| ADR | Quyết định | Bối cảnh ngắn | Trạng thái | Xét lại khi |
|---|---|---|---|---|
| 001 | Monolith Next.js + tRPC + Supabase, một Postgres | 2 dev, stack đã thạo; 11 nguồn cần một sự thật | Duyệt | >10k user hoặc >1M req/ngày |
| 002 | Dữ liệu cảm xúc lưu như dữ liệu thường: không mã hóa, không schema riêng; mood trong `evidence`, `counselor_notes` trong `care`, phân quyền ma trận chung | Chủ tịch 19/07, tái khẳng định 23/07/2026: không phức tạp hóa | Duyệt (final) — giữ 2 lời hứa công khai (không xếp loại bằng cảm xúc §5; xóa chi tiết 12 tháng); hành động kèm còn lại chỉ là văn bản: tu chính Hiến chương điều 3 | — (đã tái xác nhận, không chờ phiên Hội đồng) |
| 003 | Thiết bị = điện thoại cá nhân; PWA, chưa lên store; sau ổn định lên cả CH Play + App Store qua Capacitor | Chủ tịch 19/07, ghi đè BRD "THCS 100% laptop" | Duyệt (kèm errata BRD) | sau gate T12/2026 |
| 004 | Cờ cảnh báo là if-then đọc config, không ML | minh bạch, kiểm chứng được, chưa có dữ liệu huấn luyện | Duyệt | sau ≥2 năm dữ liệu |
| 005 | Bỏ Google Classroom; Moodle cho khóa online + ngân hàng đề | BRD Final 15/07 | Duyệt | — |
| 006 | Backup 3-2-1: PITR + pg_dump đêm (role `backup_reader` read-only, GH Actions) → R2 mã hóa age + ổ cứng trường hằng tuần | chống mất dữ liệu cấp vendor; trade-off: 1 secret read-only trong GH | Duyệt | khi trường có máy chủ nội bộ |
| 007 | Điểm danh = IP trường (server kiểm) + khung giờ + GVCN xác nhận lệch; QR động 30s là nâng cấp ngủ; bản sync muộn = `queued_late`, không tự tính chuyên cần | chống gian lận không xâm phạm (không GPS/camera) | Duyệt | nếu gian lận thực tế vượt mức GVCN xử được |
| 008 | Break-glass vùng lõi: 1 dev vắng >48h + sự cố cao → merge với 1 approve + security-agent review + hậu kiểm 72h, log và báo Chủ tịch | bus factor 2 dev | Duyệt | khi đội ≥3 dev |
| 009 | Tách dữ liệu y tế khỏi vùng mở: `evidence.meal_sleep_logs` (mở) + `care.health_logs` (lõi, RLS riêng) | y tế trẻ em là dữ liệu nhạy cảm Luật 91/2025 | Duyệt | — |
| 010 | Flag engine chỉ đọc qua signal views `care.v_signal_*`; bỏ Supabase Realtime (buồng lái fetch khi mở) | chống coupling ngầm schema; bỏ dependency thừa | Duyệt | nếu xuất hiện nhu cầu realtime thật |
| 011 | **Hub là nền tảng Super App + Mini App.** Core Data Model (users, students, teachers, parents, schools, classes, roles, permissions) là Single Source of Truth trong `core`; Mini App chỉ giữ dữ liệu nghiệp vụ, FK về core, không bản sao; một Postgres chia schema theo domain (attendance, care, health, evidence, tutor; đặt chỗ finance, social, ai); Mini App lớn tách được thành service riêng mà không đổi core model | Chủ tịch chốt: Hub không phải một ứng dụng nghiệp vụ mà là nền tảng thống nhất dữ liệu + trải nghiệm | Duyệt | khi một Mini App cần tách service |
| 012 | **Auth indirection + cách ly hạ tầng.** Supabase Auth UID → `core.users` → roles → permissions → Mini App; cấm đọc `auth.users` trực tiếp; mọi truy cập DB/Auth/Storage qua adapter tập trung — Supabase chỉ là nơi host, đổi sang Neon/self-host/Cloud SQL không viết lại nghiệp vụ | tránh khóa chặt vào nhà cung cấp; nghiệp vụ sống lâu hơn hạ tầng | Duyệt | khi đổi nhà cung cấp hạ tầng |
| 013 | **MD-07: Super App là PWA, không Flutter.** Giữ nguyên TypeScript/Next.js/tRPC; loại cả phương án Flutter thuần (Fastify + Zod→OpenAPI→Dart) và vỏ Flutter + Mini App webview | Chủ tịch chốt: không chuyển sang Flutter | Duyệt | nếu phát sinh nhu cầu native sâu (offline mạnh, sinh trắc học) — mở ADR mới, không quay lại phương án Flutter cũ |
| 014 | **Hub là Identity Provider cho hệ ngoài qua OIDC bridge.** `node-oidc-provider` mount trong cùng deployable (không service riêng); Moodle là RP đầu tiên; khớp tài khoản qua `core.id_mappings`; bridge chỉ cấp định danh, không cấp quyền gọi API Hub | Yêu cầu 24/07/2026: một định danh duy nhất — đăng nhập Hub rồi vào thẳng hệ ngoài | Duyệt | khi số RP ngoài đủ lớn cần bảng client registry + màn hình quản trị thay vì config tĩnh |
| 015 | **Chuẩn Embedded Mini App — 3 tier tin cậy (Native/Embedded/Linked).** Embed Bridge nhúng iframe qua OIDC (ADR-014) + postMessage/context token; Embed API scoped thay service_role; cấm app ngoài (kể cả build bằng Base44, Google AI Studio, Lovable) lưu định danh học sinh thật, chỉ external reference qua `core.id_mappings`; nút quay lại Hub nằm ngoài iframe | Yêu cầu 24/07/2026: chuẩn hóa việc nhúng app tự code hoặc vibe-code vào Shell, không đăng nhập lại, không rò dữ liệu ra khỏi tầm kiểm soát | Duyệt | khi có ≥5 app Tier 2 cần bảng client registry + màn hình quản trị thay vì config file |
| 016 | **Vòng đời định danh khép kín.** Đăng xuất chung (`end_session_endpoint` + back-channel logout); token ≤15 phút và mỗi lần refresh kiểm `core.users.status` ⇒ khóa tài khoản là cắt mọi hệ trong một chu kỳ; scope `hub_profile` trả `hub_role`/`hub_school`/`hub_classes` để Moodle tự xếp lớp; tách `core.identity_links` (đăng nhập, mọi loại tài khoản) khỏi `core.id_mappings` (dữ liệu, chỉ học sinh), UNIQUE cả hai chiều. Kèm theo: flag engine bỏ qua rule có nguồn hết tươi (`ops.source_freshness`) và không leo thang cờ `origin='backfill'`; thống nhất một tên `ops.job_runs` | Rà soát 27/07/2026: ADR-014 mới làm chiều mở, thiếu chiều đóng — phòng máy dùng chung giữ phiên người trước, tài khoản đã khóa vẫn vào được Moodle; `id_mappings` không có cột `user_id` nên giáo viên không map được | Duyệt | khi có RP thứ 3–4 cần bảng client registry |
| 017 | **Ba rổ dữ liệu + ổ cắm chuẩn cho app ngoài.** Xanh/Vàng/Đỏ khai trước Tier (Đỏ = `care`/`counselor_notes`/`health`/mood — cấm tuyệt đối, CI chặn); alias do Hub sinh, mỗi app một dải riêng; ghi qua `staging` chạy ngay theo sự kiện; quyền đi theo người dùng chứ không theo app, **không cấp API key cho app không có backend riêng**; `postMessage` kiểm origin + schema + timeout thay cho token trong query string; Manifest có hạn rà lại 6 tháng; kèm app mẫu + trang hướng dẫn cho vibe team | Chủ tịch 27/07/2026: app vibe-code (fitness, căn tin, bảng tin) là hướng lâu dài và sẽ có nhiều — cần chuẩn hóa rõ thay vì mỗi app một kiểu; ADR-015 chưa trả lời được "app ghi dưới danh nghĩa ai" trong khi RLS xây theo danh tính người | Duyệt | khi có ≥5 app Tier 2 (cùng mốc DEBT #8) |
| 018 | **App runtime tự host trên VPS, thay Vercel.** Next.js + tRPC chạy trên VPS riêng, PM2 + Nginx + Let's Encrypt, CI tự deploy + rollback một lệnh; **CSDL/Auth/Storage giữ nguyên ở Supabase** — không đổi ADR-011/012, chỉ đổi nơi host phần app | 28/07/2026: chi phí nền tảng quản lý sẵn (Vercel) cao hơn nhiều so với thuê máy trần cho cùng tải (3,5 req/s trung bình); đổi lại 2 dev tự viết script deploy/rollback thay vì có sẵn | Duyệt | nếu cần auto-scale ngang thật hoặc 2 dev không kham nổi vận hành VPS — sizing cụ thể xem ADR-019 |
| 019 | **Chốt hạ tầng đơn giản nhất trước go-live (sửa sizing ADR-018 + sửa backup ADR-006).** Một VPS duy nhất **16 vCPU/32 GB RAM** chạy toàn bộ app — không nhiều VPS, không load balancer, không Kubernetes. Supabase Pro lo CSDL/Auth/Storage ở compute tier mặc định (không mua thêm PITR, không thêm compute cao hơn trừ khi k6 fail). Code trên GitHub. Backup còn 2 lớp: backup hằng ngày mặc định Supabase + pg_dump hằng tuần chép thẳng vào ổ cứng trường (bỏ PITR, bỏ lớp kho trung gian R2/B2) | 28/07/2026: ưu tiên tối đa sự đơn giản và ít dịch vụ phải vận hành với 2 dev, chấp nhận VPS over-provision xa so với tải đo được (đổi lấy không phải quay lại nâng cấp/scale trong nhiều năm) và RPO tụt (24h/7 ngày thay vì phút/24h) — hệ quả RPO ghi `DEBT.md` #18 | Duyệt | nếu BGH thấy RPO 7 ngày không chấp nhận được sau khi đo thực tế, nếu VPS chạm ngưỡng tải thật (không chỉ lý thuyết), hoặc khi trường có máy chủ nội bộ ổn định hơn |
