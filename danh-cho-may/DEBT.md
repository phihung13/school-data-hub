---
ban-doi-ung: none
sync-version: 21
---

# Sổ nợ kỹ thuật — nợ không ghi sổ là nợ bị quên

| # | Món nợ | Lý do chấp nhận | Điều kiện trả / rà lại | Ngày rà |
|---|---|---|---|---|
| 1 | Hiến chương điều 3 còn ghi "mã hóa" — chưa khớp thực tế (ADR-002, tái xác nhận 23/07/2026) | quyết định 19/07 mới hơn văn bản; nợ chỉ còn là câu chữ, không phải quyết định mở | tu chính văn bản Hiến chương — không cần chờ phiên Hội đồng dữ liệu | T12/2026 |
| 2 | QR động 30s chưa xây | không xây thứ chưa cần | bật nếu gian lận điểm danh vượt mức GVCN xử được | gate T12/2026 |
| 3 | Secret DB (read-only) nằm trong GH Actions cho backup | rẻ hơn nuôi VPS; role chỉ SELECT | chuyển về máy chủ trường khi có | mỗi quý |
| 4 | Không read replica | chưa cần ở ≤5k user | >10k user hoặc >1M req/ngày | khi chạm ngưỡng |
| 5 | Điểm danh IP-based chứng minh thiết-bị-ở-trường, không phải em-ngồi-trong-lớp | giới hạn công bố; chốt là con người | cùng #2 | gate T12/2026 |
| 6 | Partition bảng lớn thiết kế sẵn nhưng chưa bật | <5M dòng chưa cần | bảng nào chạm 5M dòng | kiểm mỗi quý |
| 7 | **TRẢ MỘT NỬA 02/08/2026 — năm kịch bản đã viết đủ, năm cái còn lại vẫn ở mức một dòng.** Đã viết đủ và đã DIỄN THỬ: RB-13 (Hub mở được nhưng không bấm được) · RB-01 (không nối được cơ sở dữ liệu) · RB-14 (đường ra Internet chết) · RB-02 (bộ quét cờ không chạy đêm qua) · RB-09 (lô nạp có dòng bị từ chối). Nằm ở `07-operations.md` mục 4d cho máy, và `danh-cho-nguoi/so-tay-su-co.html` cho người trực (in A4, mỗi kịch bản một trang, chiều cao từng trang đã đo chứ không ước lượng). Mỗi kịch bản có đủ năm phần: dấu hiệu bằng lời người dùng sẽ dùng · đúng một lệnh xác nhận · các bước chữa · phép kiểm KHÔNG chấp nhận "mở thử thấy được" · ranh giới gọi dev.<br>**Còn nợ:** RB-03 tới RB-08 và RB-10 vẫn là một dòng mô tả | Chọn năm cái này vì bốn cái đầu **đã xảy ra thật** trong hai ngày 31/07–02/08/2026 và có phép đo trong chính sổ này, cái thứ năm là việc bắt buộc phải làm trước khai giảng. Năm cái còn lại chưa xảy ra lần nào, nên viết chúng bây giờ là viết bằng phỏng đoán — đúng thứ làm runbook chỉ sang một cái nút không tồn tại (RB-08 từng bảo người trực bấm PITR, thứ đã bị bỏ từ 28/07) | Cổng go-live mục 8, nay đòi thêm hai điều đo được: năm kịch bản đã **in ra giấy** dán ở phòng trực, và người trực đã **tự diễn thử ít nhất RB-01 và RB-14** một lượt có người đứng xem. Đọc runbook không phải là biết dùng runbook | năm cái còn lại: tuần 6 |
| ~~8~~ | ~~App Manifest (embedded apps, ADR-015) quản lý bằng file config, chưa có bảng + màn hình quản trị~~ | **ĐÃ TRẢ 02/08/2026** (migration `0052`). Ngưỡng cũ là "xây khi có ≥5 app Tier 2" — bỏ ngưỡng đó trước khi tới, và lý do không phải là số app: thêm app phải deploy thì bất tiện, **TẮT app phải deploy thì nguy hiểm**. App ngoài lộ dữ liệu hoặc domain hết hạn rồi bị người khác mua lại thì đường thu hồi nhanh nhất vẫn phải chờ hai dev mở máy. Nay `core.embedded_apps` + màn `/quan-tri/mini-app`: tắt là một nút, hiệu lực ngay lượt request kế tiếp. Kèm theo, `review_due_on` cho luật "quá hạn không rà thì thu hồi quyền" (`08-embedded-apps.md` mục 5) một chỗ để tồn tại — trước đó luật ấy chưa từng chạy được ngày nào vì không có cột nào ghi ngày | — | — |
| 9 | Chưa có UI cảnh báo cụ thể cho Tier 3 (mở tab ngoài) trước khi điều hướng rời Hub | thiết kế UI chưa làm, tính năng nhúng chưa có app Tier 3 thật | thiết kế trước khi app Tier 3 đầu tiên go-live | khi có app Tier 3 đầu tiên |
| 10 | Chưa có cron tự dò lại `X-Frame-Options`/CSP của các app Tier 2 đã duyệt (nền tảng ngoài có thể đổi chính sách bất kỳ lúc nào) | rà thủ công theo mục 7 của `08-embedded-apps.md` là đủ ở số lượng app hiện tại | tự động hóa khi số app Tier 2 đủ lớn để rà tay không xuể | khi có ≥5 app Tier 2 |
| 11 | Chưa có cron nhắc hạn rà lại App Manifest 6 tháng, và chưa có quy trình tự động thu hồi alias/key của app đã ngừng dùng (ADR-017 mục 5) | số app hiện tại đếm trên đầu ngón tay, lịch tay của dev bảo trợ là đủ | tự động hóa cùng đợt với UI quản trị Manifest | khi thêm app thứ 5 |
| ~~12~~ | ~~Bộ mẫu cho vibe team (ADR-017 mục 1.4)~~ | **ĐÃ TRẢ 29/07/2026** — Tier 2 (nhúng iframe qua Embed Bridge) đã chạy thật đầu-cuối với RP thật đầu tiên (Factory, factory.vietanh.org): SSO Đường A, redirect_uri thứ hai cho embed, postMessage `embed:ready`/`embed:token`/`embed:resize`/`embed:error`, Đường B qua `ops.embedded_app_events` (rổ Xanh, cổng nhận chung), logo tự host. Hạ tầng Hub (`/embed/[appId]`, `embed-frame.tsx`, `middleware.ts`, `registry.ts`) đã tổng quát hoá — app thứ hai chỉ cần đăng ký, không viết code hạ tầng mới. Trang hướng dẫn duy nhất: `templates/huong-dan-embed-tier2.md`, kèm 6 bẫy kỹ thuật thật đã gặp (CSP tự chặn, đua tranh embed:ready, thiếu iss RFC 9207, double-invoke đổi token, hydration mismatch do chặn quảng cáo, sandbox thiếu allow-same-origin) | — | — |
| ~~13~~ | ~~`packages/core/contracts` chưa có số phiên bản + changelog~~ | **ĐÃ TRẢ 31/07/2026** — `contracts/version.ts` (`CONTRACTS_VERSION`) + `contracts/CHANGELOG.md` theo Keep a Changelog, và `tools/contracts-lint.mjs` chụp lại bề mặt Zod: đổi contract mà không ghi sổ là CI đỏ, không phải là một lời nhắc trong review. Hai gói của đợt B (`man-hinh-tam-ly-cum`, `gvcn-nhieu-lop`) đã đi qua đúng cổng đó — cả hai chỉ THÊM field, ghi rõ trong changelog vì sao không được "hợp nhất hai schema cho gọn" | — | — |
| ~~14~~ | ~~Migration 0010–0012 chưa chạy được vì thiếu baseline~~ | **ĐÃ TRẢ 27/07/2026** — baseline `0001`–`0009` đã viết, 12 migration chạy sạch trên PostgreSQL 16 thật, 94 assertion pgTAP xanh, chạy lại migration cho ra schema y hệt (1203 dòng DDL khớp) | — | — |
| 15 | CI đã đủ cổng nhưng **chưa từng chạy một lần nào trên runner sạch**. Bộ 7 job đã đầy đủ tính tới 31/07/2026: đồng bộ hồ sơ · schema-lint (§1/§2/ADR-011) · secret-scan (§4) · typecheck (`pnpm -r typecheck` + `tsconfig.tests.json`) · unit (vitest) · build (`next build`) · database (migrations → pgTAP → diff schema → seed → vitest tests/db trên Postgres thật). **Trả một phần 31/07/2026:** bước (1) xong — repo đã có commit đầu (`624e402`) và đã có remote GitHub (`origin`), nên 7 job không còn là văn bản thuần. **Còn nợ bước (2) và (3)** | Toàn bộ kiểm chứng tới nay chạy tay trên máy dev, và **kết quả tay khớp** (31/07/2026, đo bằng `tools/run-db-tests.sh` trên database dựng lại từ đầu: 358 assertion pgTAP xanh trên 32 file, 34 migration chạy sạch). Cái thiếu không phải là nội dung cổng mà là nơi đặt cổng | Hai việc còn lại, **trong cùng một buổi**: (2) mở một PR nháp để thấy đủ 7 job xanh thật — job chạy trên máy dev không chứng minh được nó chạy trên runner sạch (đặc biệt job `database` cần container Postgres + pgTAP do runner tự dựng, và job `typecheck` cần `pnpm install` từ lockfile chứ không phải `node_modules` đang có sẵn trên máy dev); (3) bật branch protection cho `main` + CODEOWNERS (§10 hai chữ ký). Bỏ bước (3) thì 7 job vẫn bỏ qua được bằng một lần merge thẳng | ngay trong tuần |
| ~~16~~ | ~~Ma trận RLS chưa phủ `principal` và `board`~~ | **ĐÃ TRẢ 31/07/2026** — `0023_principal_scope_test.sql` khẳng định hiệu trưởng thấy đúng cơ sở mình và KHÔNG thấy cơ sở khác ("campus" không phải "toàn hệ"), `0040_report_aggregate.sql` mở đường đọc tổng hợp cho cả hai vai (`report.class_pulse`/`grade_pulse`, ngưỡng ẩn danh dùng chung `report.min_cohort()` = 10), kèm `0040_report_aggregate_test.sql` và `tests/db/bgh-tong-hop.test.ts`. Cổng vai nằm ở CẢ hai tầng: `roleProcedure("principal","board")` ở API và `report.aggregate_school_ids()` RAISE ở DB — gọi sai vai nhận LỖI, không nhận bảng rỗng (bảng rỗng đọc y hệt "cả khối hôm nay không có gì") | — | — |
| 17 | `evidence.meal_sleep_logs` (ADR-009 nói vùng mở) đã đặt trong schema `health` theo `02-database.md`; hai văn bản dùng tên khác nhau cho cùng một thứ. Baseline theo `02-database.md` vì đó là bản chi tiết nhất và mới nhất | không chặn việc gì, chỉ là câu chữ | thống nhất tên trong ADR-009 hoặc ghi errata | T12/2026 |
| 18 | RPO tụt: từ ~phút (PITR) xuống ≤24h (backup mặc định Supabase); từ ≤24h (có lớp R2) xuống ≤7 ngày (thảm họa cấp nhà cung cấp, chỉ còn bản pg_dump tuần) — do bỏ PITR và bỏ lớp R2 trung gian (ADR-019, sửa ADR-006) | quyết định 28/07/2026 ưu tiên đơn giản/ít dịch vụ phải vận hành với 2 dev hơn RPO tối ưu; **đã báo BGH** | tăng tần suất pg_dump lên hằng ngày trên VPS ứng dụng (không cần hạ tầng mới) nếu BGH thấy 7 ngày là rủi ro quá cao, hoặc bật lại PITR khi ngân sách/nhân sự cho phép | rà theo học kỳ hoặc ngay sau một sự cố thật |
| 19 | **ĐÃ KHOÁ CỬA 02/08/2026 (ADR-028) — nợ chuyển hình dạng, chưa xoá.**<br>*Nợ gốc:* cửa đăng nhập dev (`/api/auth/dev-login`) mở ra Internet qua tunnel `hub.truongvietanh.com`: POST kèm một `auth_uid` fixture (dãy `90000000-…-000N`, đoán được) là nhận phiên hợp lệ, không mật khẩu. Đo thật 30/07/2026 từ ngoài vào, và đo lại lần nữa 02/08/2026 khi thử ngược bản vá (gỡ cửa ⇒ `POST https://hub.truongvietanh.com/api/auth/dev-login` không mã trả **200**). Từ 31/07/2026 sau cửa đó còn có `admin.hung` mang vai `principal` — nhìn được toàn bộ học sinh cơ sở Q7. Cửa này cũng là cửa vào Factory, vì Factory tin Hub làm nhà cung cấp định danh.<br>*Đã trả:* route đòi một bí mật dùng chung đặt ở `DEV_LOGIN_SECRET` (chỉ trong `apps/hub/.env.local`, đã gitignore); nhập một lần trên `/login`, nhớ bằng cookie riêng `hub_dev_gate` (HMAC, 30 ngày, HttpOnly, Secure khi đi https); thiếu biến ⇒ **503 đóng với tất cả**; `NODE_ENV=production` ⇒ **404**; 5 lần thử/phút/IP. Không có nhánh loopback (xem ADR-028: đường hầm làm mọi request từ Internet trông như đến từ 127.0.0.1). Đo sau khi vá, qua CẢ HAI đường: không mã 401 · mã sai 401 · mã đúng 200 · sau khi mở khoá, điện thoại (khung 375×812, tên miền công khai) vào thẳng danh sách tài khoản và đăng nhập được.<br>*Còn nợ, ba việc:* (1) đây là **mật khẩu dùng chung**, không phải danh tính — `ops.audit_log` biết "một phiên vai hiệu trưởng đã mở", KHÔNG biết ai mở; (2) mã chỉ thu hồi được bằng cách đổi biến môi trường rồi khởi động lại Hub (không có danh sách thiết bị, không revoke từng máy); (3) chưa bọc Cloudflare Access — vẫn là cách duy nhất cho ra **danh tính người thật** mà không phải chờ Google/Zalo OAuth | chủ đầu tư quyết định chấp nhận 31/07/2026: đang giai đoạn test, dữ liệu trong DB là dữ liệu giả (seed), ưu tiên tốc độ thử nghiệm hơn phiền toái đăng nhập. Từ 02/08/2026 lý do chấp nhận **hẹp lại**: chấp nhận một mật khẩu dùng chung cho tới ngày có OAuth thật, KHÔNG còn chấp nhận cửa mở | **Điều kiện chặn đã hạ một bậc, chưa gỡ.** Cửa mở toang không còn là lý do chặn nạp dữ liệu thật; **ba việc còn nợ ở trên thì có**: trước ngày nạp danh sách học sinh thật phải có định danh từng người (Cloudflare Access bọc tên miền — không đụng code, tunnel sẵn có), hoặc bỏ hẳn dev-login khi Google/Zalo OAuth thật lên (`10-mua-sam-ha-tang.md`). Cổng canh hằng ngày đã có: `tools/start-local.sh` bước 2c tự gõ cửa không cầm mã và **dừng script** nếu nhận 200; `tests/unit/dev-login-gate.test.ts` (19 ca) canh phần logic + việc cửa còn được mắc trong route + việc mã thật không rò sang file nào git theo dõi | rà lại ngay khi có lịch nạp dữ liệu thật |
| 20 | Mở cửa vườn ươm 05/09/2026 khi **chưa kiểm định an ninh độc lập (pentest)** và **chưa có hồ sơ DPIA**. Cả hai là cổng go-live bắt buộc theo `07-operations.md` mục 7 nhóm 4 và 5; DPIA còn là nghĩa vụ theo Luật 91/2025, nên đây là khoảng **chưa tuân thủ**, không chỉ là rủi ro kỹ thuật | Chủ tịch quyết định 31/07/2026: không chờ hai hạng mục này để kịp mốc khai giảng, vì mốc khai giảng không lùi được. Giảm thiệt hại bằng phạm vi hẹp: 1–2 lớp, dữ liệu tối thiểu | **Không mở rộng quá hai lớp cho tới khi trả xong cả hai.** Đặt lịch pentest và giao đầu mối pháp lý làm DPIA ngay sau khi thí điểm chạy ổn định (dự kiến T10/2026) | rà lại ngay khi có đề xuất mở thêm lớp |
| ~~21~~ | ~~Ngữ nghĩa cờ E_MOOD chưa thống nhất: tham số tên `negative_days_streak` (ngụ ý LIÊN TIẾP) nhưng view `care.v_signal_emotion` đếm số ngày bất kỳ trong cửa sổ 14 ngày~~ | **ĐÃ TRẢ 31/07/2026.** Câu hỏi nghiệp vụ được chủ đầu tư chốt: **E_MOOD = 5 ngày mood xấu LIÊN TIẾP**, chuỗi thật (ADR-023). Thi hành trong `0026_care_router.sql`: `care.thresholds.params` có trường `mode` nhận `"streak"` \| `"window"` (**mặc định `"streak"`**) và view khai thêm cột `negative_streak` bên cạnh `negative_days` — nên đổi cách đếm về sau chỉ là một câu UPDATE, không phải một lần sửa mã (mệnh lệnh 7, §6). `apps/hub/server/care-thresholds.ts` đã bỏ ghi chú "chưa thống nhất"; `02-database.md` v17 đã ghi. **Còn đúng một dòng tài liệu chưa dọn:** `04-flag-engine.md` vẫn in `{"negative_days_streak": 5}` không có `mode` — file đó nằm ngoài phạm vi gói `dong-bo-tai-lieu`, đã bàn giao lại cho chủ sở hữu (nợ #26) | — | — |
| 22 | Rate limit (`apps/hub/lib/rate-limit.ts`) đếm bằng token bucket **trong bộ nhớ tiến trình**, không phải bộ đếm dùng chung | Hub là modular monolith chạy MỘT deployable (`01-architecture.md` §7): mọi request đi qua đúng một tiến trình Node nên bộ đếm RAM chính là bộ đếm toàn hệ. Thêm Redis lúc này là thêm một dịch vụ phải vận hành cho hai dev mà không thêm độ đúng nào. Bộ đếm cũng mất sạch khi tiến trình khởi động lại — chấp nhận được với hạn mức tính theo phút | **Ngày Hub chạy nhiều hơn một instance** (rolling deploy, autoscale, hay chỉ là chạy hai tiến trình sau một load balancer) thì mỗi instance đếm riêng và hạn mức thật nhân lên theo số instance — lúc đó bắt buộc chuyển sang bộ đếm chung (Redis hoặc bảng Postgres). Nguy hiểm nhất là cửa mã mời 6 ký tự `/api/auth/invite`: nhân hạn mức lên là nhân tốc độ dò mã lên | rà khi đổi mô hình triển khai |
| ~~23~~ | ~~**Không có bộ chạy migration và không có sổ ghi migration đã chạy.** Không bảng `schema_migrations`, không công cụ nào ngoài vòng lặp `for f in migrations/*.sql` trong `tools/run-db-tests.sh` — vòng lặp đó chỉ chạy được lên database RỖNG. Đo thật 31/07/2026: chạy lại bộ migration lên `hub_dev` (đã migrate) dừng ngay ở `0002` với `relation "school_networks" already exists`. Nghĩa là hôm nay **không có đường nào áp một migration mới lên database đang sống** ngoài việc mở psql chạy tay đúng file đó và tự nhớ đã chạy tới đâu~~ | **ĐÃ TRẢ 02/08/2026** (gói `flags-detail-va-bo-chay-migration`). Sổ: `ops.schema_migrations` ở `0050_so_ghi_migration.sql` — `version` (PK) · `filename` · `checksum` (sha256 hex nguyên văn byte) · `applied_at` · `applied_by` · `duration_ms` · `nhan_no` · `ghi_chu`, ba ràng buộc CHECK chặn ba kiểu dòng-sổ-nói-dối. Bộ chạy: `tools/migrate/migrate.mjs` (`status` \| `up [--dry-run]` \| `baseline --to=NNNN`), tài liệu ở `tools/migrate/README.md`. Đủ năm việc giao: biết đã áp tới đâu · từ chối áp lại · **bắt file đã áp mà nội dung đổi** (so băm, TỪ CHỐI CHẠY chứ không cảnh báo) · một transaction thật (câu ghi sổ chèn vào ngay trước `commit;` của chính file — mở transaction ở phía Node thì `commit;` bên trong commit luôn transaction của bộ chạy, đã đo) · chế độ chỉ-xem. Ghi `ops.job_runs` (`job_name='migrate'`), **cố ý không** khai vào `ops.job_schedule`. Bước **nhận nợ ban đầu** có cổng chặn hai chiều: `up` từ chối khi sổ trống mà database đã có `core.users`; `baseline` từ chối trên database rỗng | **Đã chạy thật trên `hub_dev`**: `up` → TỪ CHỐI, `baseline --to=0049` → 49 dòng `nhan_no=true` + `0050` chạy thật 45 ms, `up` → "50/50 file đã ở trong sổ". Kiểm chứng: pgTAP `0050_so_ghi_migration_test.sql` **14 assertion**; `tests/db/migrate.test.ts` **17 ca** trên ba database tự dựng tự xoá, có cả corpus giả lẫn corpus thật 50 file. Thử ngược: sửa một dòng chú thích vào `0031` đã áp → `up` và `status` đều mã thoát 1 kèm `LỆCH BĂM`; hoàn nguyên → xanh. Cùng cách với: xoá file đã áp · hai file cùng số · file thiếu `begin;/commit;` · migration hỏng giữa chừng (không đối tượng nào ở lại, không dòng sổ nào ở lại, `ops.job_runs` ghi `failed`). **Runbook đã trả 02/08/2026 ở lượt gộp:** `07-operations.md` có **RB-11** (năm bước, bước 1 là `status` chứ không phải `up` — hỏi trước khi làm), có mục 4b mô tả bước nhận nợ, và cổng go-live nhóm 8 nay đòi *"RB-11 đã chạy thật một lượt trên máy chủ đích"*. Cùng lượt đó sửa một chỉ dẫn sai trong RB-08: nó bảo người trực *"PITR về trước thời điểm chạy"* trong khi **PITR đã bị bỏ từ 28/07/2026** (ADR-019) — một runbook chỉ sang một cái nút không tồn tại, đọc lúc 2 giờ sáng. **Còn lại đúng một việc:** cổng CI gọi `migrate.mjs status` trên database staging | — |
| ~~24~~ | ~~**Hai hàm giữ lời hứa "xóa chi tiết cảm xúc sau 12 tháng" đã có, nhưng chưa có gì gọi chúng hằng đêm.**~~ | **ĐÃ TRẢ PHẦN MÃ 31/07/2026** (phần cắm lịch lên máy thật chuyển thành nợ #33) — `0041_job_schedule.sql` + `tools/jobs/run-all.mjs`: `ops.job_schedule` khai job nào chạy bao lâu một lần, `ops.v_job_health` cho **bảy** trạng thái trong đó `chua_chay` là trạng thái RIÊNG chứ không phải `ok`, `ops.reap_stale_runs()` biến dòng `running` treo thành `failed` thấy được. Bốn dòng đã khai: `emotion_retention` (lời hứa 12 tháng), `homeroom_drift` (đọc `ops.v_homeroom_drift` của `0030`), `flag_engine` (`0039`), và `job_scheduler` — **chính bộ lịch tự khai một dòng cho mình**, nên máy chạy cron chết là `qua_han` sáng lên, không phải im lặng tuyệt đối. Luật chép từ `0011`: chỉ khai job đã có bộ chạy, viết thành mệnh đề `WHERE to_regprocedure(...) is not null` chứ không nằm trong comment | — | — |
| ~~25~~ | ~~**Vai `counselor` (tâm lý cụm) có quyền DB đầy đủ nhưng không có đường API nào dùng được.**~~ | **ĐÃ TRẢ 31/07/2026** (gói `man-hinh-tam-ly-cum`) — hai màn thật: `/tam-ly` (hộp việc của cụm) và `/tam-ly/ho-so/<em>` (hồ sơ một em), sau cổng `counselorProcedure` = `roleProcedure("counselor")` chứ KHÔNG phải `careStaffProcedure`: phạm vi hai màn đó là CỤM, không phải một lớp. Thẻ mini app "Tâm lý cụm" chỉ hiện cho đúng vai — thẻ bấm vào ra `FORBIDDEN` là menu nói dối, và menu 404 cũng vậy. Hợp đồng Zod cố ý KHÔNG cho `ClusterHelpSignal` mang cột `note`: đường chuyển tuyến "em đồng ý cho phòng tâm lý đọc" chưa tồn tại (xem ADR-025) | — | — |
| ~~26~~ | ~~`04-flag-engine.md` in tham số E_MOOD thiếu trường `mode`~~ | **ĐÃ TRẢ 31/07/2026** — `04-flag-engine.md` nay in `{"negative_days_streak": 5, "mode": "streak"}`, mô tả đúng khoá mới của `care.thresholds` (theo từng cơ sở, đọc qua `care.resolve_threshold`), đúng `care.rules.source_key` của `0039`, và sửa luôn một câu sai to hơn: file cũ ghi "chạy pg_cron 01:00 mỗi đêm" trong khi bộ chạy thật là `ops.job_schedule` + `tools/jobs/run-all.mjs` (`0041`) — pg_cron chưa bao giờ được bật. Kèm bảng "đã cài đặt thật tới đâu", nơi C_CEFR và buồng-lái-đọc-cờ được ghi là CHƯA thay vì im lặng. **Còn một nửa của luật đồng bộ:** file có bản đôi, đã tăng `sync-version` 7→8 phía máy, phía `ho-so-he-thong.html` do gói khác sở hữu (xem canPhoiHop) | — | — |
| 27 | **Bộ lưu trạng thái của `oidc-provider` là adapter in-memory mặc định.** Mọi phiên OIDC, authorization code, refresh token sống trong RAM của một tiến trình | Cùng lý do như nợ #22 (rate limit): Hub chạy MỘT deployable, nên "bộ nhớ tiến trình" = "bộ nhớ toàn hệ". Khác biệt so với #22: ở đây mất trạng thái không chỉ là mất bộ đếm — **khởi động lại tiến trình là đăng xuất toàn bộ người đang dùng khỏi mọi hệ ngoài** | Ngày chạy >1 instance **hoặc** ngày dùng rolling deploy (deploy không được làm rơi phiên của cả trường giữa giờ học). Khi đó cần adapter lưu ra Postgres — thư viện có sẵn giao diện, việc là viết ~150 dòng + bảng | rà cùng nợ #22, khi đổi mô hình triển khai |
| 28 | **Bộ đếm số lần nhập sai mã mời phụ huynh nằm trong RAM** (`apps/hub/server/oidc/invite-guard.ts`), không nằm trong database, và mã mời **chưa có đường thu hồi**: không cột `revoked_at`, không `max_uses`, không đếm lần sai lưu bền | Cửa mã mời đã có rate limit theo IP (10 req/phút) nên không phải cửa mở toang; bộ đếm RAM là lớp thứ hai. Dữ liệu hiện là dữ liệu giả | Một migration thêm `revoked_at` / `max_uses` / `failed_attempts` vào `core.parent_invite_codes`, cho `core.redeem_parent_invite_code()` đếm lần sai, và sau lần redeem đầu chỉ nhận lại trong 15 phút (đủ cho retry §9) rồi khóa hẳn. Khi có, hạ bộ đếm RAM xuống còn lớp đệm. **Bắt buộc trước khi phát mã mời cho phụ huynh thật** — mã 6 ký tự không thu hồi được là mã sống mãi | cùng mốc với nợ #19 |
| 29 | **Chưa có hàng đợi thử lại cho back-channel logout.** RP đang tạm chết lúc Hub gửi tín hiệu đăng xuất thì tín hiệu đó mất luôn, không ai thử lại | Mới có một RP thật (Factory) chạy cùng máy; xác suất lệch thấp và hậu quả hiện tại là "phiên phía RP sống thêm tới khi token hết hạn" (≤15 phút), không phải "sống vô hạn" | Khi có RP thứ hai nằm ngoài mạng nội bộ. Hình thức đúng là dùng lại `ops.outbox_messages` đã có (`dedup_key` + `sent_at`), không dựng cơ chế thứ hai | khi thêm RP ngoài mạng nội bộ |
| 30 | **`crypto.randomUUID()` được vá thủ công ở ba chỗ khác nhau** (`gvcn-dashboard.tsx`, `lib/offline-queue.ts`, `components/gvcn/mutation-id.ts`) thay vì một chỗ | Hàm này chỉ tồn tại trong secure context; mở Hub bằng `http://<ip nội bộ>` (đúng kịch bản demo trong trường) mà gọi thẳng là `TypeError` lúc render — **trắng cả màn hình**, không phải lỗi nhỏ. Ba bản vá đều đúng, chỉ là ba bản | Gộp về `components/gvcn/mutation-id.ts` (bản đầy đủ nhất) và xóa hai bản kia. Việc nhỏ, xếp vào lần dọn frontend kế tiếp | lần dọn frontend kế tiếp |
| ~~31~~ | ~~**`apps/hub/lib/offline-queue.ts` dừng flush ngay khi gặp lỗi đầu tiên** (`break`), nên một item hỏng vĩnh viễn (ví dụ 401 hết phiên) **chặn luôn mọi check-in xếp sau trong hàng đợi, mãi mãi**~~ | **ĐÃ TRẢ 02/08/2026** (gói `hang-doi-offline-khong-tac`) — `flushQueuedCheckins` nay cho mỗi bản ghi đúng BA đường ra và không đường nào im lặng: gửi trọn vẹn → rời hàng đợi · lỗi tự khỏi (mạng/5xx/408/429) → ở lại chờ · hỏng vĩnh viễn → rời hàng đợi KÈM dấu vết em đọc được (`listFailedCheckins`, màn /checkin hiện ra, chỉ nút "Đã hiểu" mới xoá). Luật phân loại lỗi dời khỏi `checkin-view.tsx` vào chính hàng đợi — một bản luật, hai nơi import, hết cảnh hai bản lệch nhau | Trả kèm một khe **chưa từng ghi trong sổ này**: flush cũ gọi `submit()` xong là `dequeueCheckin` ngay, KHÔNG đọc `moodSaved`. Sau `0047` một lượt gửi trả 2xx với `moodSaved=false` (nhà em chưa có phiếu đồng ý) — hàng đợi đánh dấu "đã gửi" cho một mức tâm trạng máy chủ từ chối ghi. Đo thật 02/08/2026 bằng phiên của Minh: `POST /api/trpc/checkin.submitMood` trả `{"moodSaved":false,"moodBlockedReason":"chua_co_phieu_dong_y"}` — tức đây là ĐƯỜNG ĐANG CHẠY, không phải giả thuyết. Nay ca đó cũng rời hàng đợi kèm dấu vết riêng. Khoá §9: hai lượt flush chồng nhau dùng chung một lượt chạy; gọi `submitMood` hai lần bằng HTTP thật cho ra đúng một dòng (cùng `checkinId`). Cổng: `tests/unit/offline-queue.test.ts` (10 bài, đã thử ngược từng bài) + 3 cổng quét nguồn trong `tests/unit/checkin-view.test.ts` | — |
| 32 | **Hai đường tính cờ đang chạy song song.** `0039` đã cài `care.run_flag_engine()` ghi `care.flags`/`care.care_cases`, nhưng `care.getDashboard` vẫn **tự tính tín hiệu thô mỗi lần một GVCN mở buồng lái** — không đọc `care.flags` một dòng nào.<br>**TRẢ MỘT NỬA 01/08/2026** — nửa "dòng Quét đêm qua" đã xong và xong hẳn: `getDashboard` trả `scanHealth` đọc từ `ops.v_job_health`, buồng lái có **dải trạng thái cố định ở đầu trang hiện MỌI LÚC** (trước đó mốc quét chỉ vẽ ở nhánh bảng trống, có một cờ là nó biến mất), chín trạng thái ra chín câu, `chua_chay`/`khong_doc_duoc` không bao giờ được in "lớp mình đang ổn", và luật bị bỏ qua trong lượt quét hiện thành chữ. Kèm một mâu thuẫn cũ được vá: lớp còn hồ sơ chăm sóc mở thì thôi nhận câu "hết việc rồi" (đo được ở 6A2: 1 hồ sơ mở + 0 cờ + quét tươi → bản cũ in "lớp mình đang ổn" ngay cạnh ô "1 hồ sơ chăm sóc đang mở").<br>**NỬA CÒN NỢ là chính nó: bên đọc vẫn chưa chuyển.** | Cố ý, và là cách đối chiếu duy nhất có: đổi bộ tính VÀ đổi bên đọc trong cùng một lượt thì lúc hai bên ra số khác nhau không biết bên nào sai. Giữ song song để so trước, chuyển sau.<br>**Phép so đã chạy 01/08/2026 và cho ra KHÔNG KHỚP** — nên lý do hoãn hôm nay không còn là "chưa đủ một tuần dữ liệu", nó là bốn chỗ chặn có tên ở cột bên phải | **ĐÃ ĐO, đây là số:** chạy `care.run_flag_engine(current_date,'live')` trên `hub_dev` (lần chạy lại `flags_new=0` — §9 đúng), rồi FULL OUTER JOIN tập (học sinh × mã luật) của hai đường. Đo **ba lượt** trong ngày (trước reseed · sau reseed · sau reseed lần hai): **7/10 · 7/11 · 6/11 trùng**. **Con số trùng đổi theo seed và theo thời điểm chạy engine — đừng lấy làm mốc nghiệm thu.** Kết luận thì không đổi qua cả ba lượt: **0 dòng chỉ có ở buồng lái**, và **mọi dòng lệch đều nằm phía `care.flags`** — `A_ATTENDANCE` của bốn em (Trần Thị Bình 6A2, Lê Gia Bảo 6A3, Phạm Gia Bảo 6A4, Hoàng Gia Bảo 6A5), cộng ở lượt ba một `E_URGENT` (Nguyễn Văn Minh 6A1) mà chính nó là **chốt chặn (b) hiện ra sống**: engine chạy trước, `help_requests` đổi sau, `care.flags` còn giữ cờ trong khi buồng lái đã thôi tính. Phần `E_MOOD`/`E_URGENT` khớp tuyệt đối. Bốn chỗ phải xong TRƯỚC khi chuyển, không chỗ nào là việc sửa câu SQL:<br>**(a) MỞ RỘNG PHẠM VI, cần người quyết.** Buồng lái chỉ sinh hai mã (`help_requested ? 'E_URGENT' : 'E_MOOD'`), không có nhánh nào đọc chuyên cần/hành vi. Chuyển bên đọc = GVCN lần đầu thấy cờ `A_ATTENDANCE`/`B_BEHAVIOR`. Lọc im lặng hai mã đó để "giữ nguyên hiện trạng" là biến bốn em đang có tín hiệu chuyên cần thành bốn em vô hình — nếu buộc phải lọc thì màn hình phải nói "còn N cờ loại khác chưa hiển thị".<br>**(b) NHỊP ĐÊM NUỐT CỜ KHẨN.** `ops.job_schedule` khai `flag_engine` chạy mỗi 1 ngày (dung sai 6h). Thí nghiệm trong `BEGIN…ROLLBACK`: chèn một `help_requests` khẩn SAU lần quét → buồng lái tự tính thấy ngay, `care.flags` không có gì cho tới đêm sau. Em tự bấm "cần gặp thầy cô" là đúng loại tín hiệu không chịu được độ trễ một đêm. Phải chọn tường minh: giữ `E_URGENT` tính trực tiếp (nó là một join nhẹ, không phải phép quét nặng mà nợ này muốn bỏ) rồi **nói ra trên màn rằng hai nguồn có hai nhịp**, hoặc cho `help_requests` một trigger/outbox gọi engine ở chế độ chỉ-`E_URGENT`.<br>**(c) `detail` SNAKE_CASE ↔ UI CAMELCASE.** Engine ghi `help_requested`/`negative_streak`/`nguong`; `gvcn-dashboard.tsx` đọc `helpRequested`/`negativeDays`. Chạy thử ba hàm thuần với `detail` thật: mọi cờ rơi xuống câu mặc định "Tín hiệu cảm xúc cần để ý", cờ `A_ATTENDANCE` bị dán nhãn "CẢM XÚC ĐI XUỐNG NHIỀU NGÀY", và nút "Cô đã gặp em rồi" **biến mất** vì `detail.helpRequested === true` là false với mọi cờ khẩn. Map một chiều tại ĐÚNG MỘT CHỖ trong `care.ts`, **không sửa `detail` trong DB** — đó là bản ghi của engine, đổi nó là đổi lịch sử.<br>**(d) `as_of_date` ĐỔI NGHĨA.** Buồng lái đặt `as_of_date` = ngày CỦA TÍN HIỆU; engine luôn đặt = ngày CHẠY. Nút "Cô đã gặp em rồi" gửi thẳng `flag.asOfDate` làm `requestedOn`, và mutation `update … where requested_on = $2 and handled_at is null` rồi trả `alreadyHandled: updated === 0`. Ba em trên `hub_dev` đang có HAI yêu cầu treo (31/07 và 01/08), nên ngay hôm nay bấm một lần chỉ tắt được một dòng, bấm lần hai nhận "Người khác đã xử lý trước rồi" trong khi yêu cầu kia còn nguyên. Sau khi chuyển thì lệch ngày làm nó khớp 0 dòng mọi lần. Phải bỏ hẳn việc suy ngày yêu cầu từ `asOfDate` (đưa `requestedOn` thật vào `detail`, hoặc nhận `helpRequestId`), và mutation **không được gộp** "đã có người xử lý" với "không tìm thấy dòng nào".<br>~~**(e) NGƯỠNG LỆCH TẦNG**~~ **ĐÃ TRẢ 01/08/2026 (`0042_nguong_theo_co_so.sql`)** — `care.v_signal_emotion` nay gọi `care.resolve_threshold('E_MOOD', s.school_id)` qua `cross join lateral`, đúng khuôn `0039` đã dùng cho `v_signal_attendance`/`v_signal_behavior`; `bad_mood_max` đi theo TỪNG DÒNG check-in thay vì nhân chéo một ngưỡng cho cả bảng. Kiểm chứng: `0042_nguong_theo_co_so_test.sql` 11 assertion xanh trên database dựng lại từ đầu, và **thử ngược** (dựng lại bản `0026` cũ rồi chạy lại đúng bài đó) cho **4 assertion đỏ** — nghĩa là bài test bắt đúng lỗi, không xanh vì may. Bài test tự khai một dòng ngưỡng riêng cho cơ sở Q2 rồi khẳng định view đổi theo, kèm một em ở cơ sở khác làm đối chứng: không có bước khai đó thì bài test xanh y hệt trên cả bản cũ lẫn bản mới | (a) chờ chủ đầu tư chốt phạm vi · (b)–(d) trước khi chuyển bên đọc · (e) đã trả |
| 33 | **Lịch job đã khai trong database nhưng chưa cắm vào máy nào.** `ops.job_schedule` (`0041`) có 4 dòng và `tools/jobs/run-all.mjs` chạy được bằng tay, nhưng chưa có Task Scheduler/cron thật gọi nó theo giờ.<br>**TRẢ MỘT NỬA 01/08/2026 — nửa "công cụ" xong hẳn:** `tools/jobs/run-all.cmd` + `run-all.sh` (đọc `DATABASE_URL` từ file env **ngoài kho**, gọi node bằng đường dẫn tuyệt đối, log ra `C:\ProgramData\hub\` / `/var/log/hub/`), `dang-ky-lich.ps1` + `dang-ky-lich.sh` (soát điều kiện rồi **CHỈ IN** câu lệnh; muốn đăng ký thật phải thêm `-XacNhan`/`--xac-nhan`), `kiem-tra-lich.ps1` + `kiem-tra-lich.sh` (hỏi ba câu: máy có tác vụ chưa · chạy lần cuối lúc nào, mã kết quả bao nhiêu · `ops.v_job_health` nói gì). Chuỗi đầu-cuối đã chạy thật qua `run-all.cmd` trên một database sạch: 4 job **rời khỏi `chua_chay` sang `ok`**, `--check` thoát `0`. Đồng thời sửa một lỗ bảo mật trong chính README cũ: mẫu `schtasks` cũ nhét `DATABASE_URL` vào tham số `/TR`, mà chuỗi đó đọc được bằng `schtasks /Query /XML` và nằm trong file XML dưới `%WINDIR%\System32\Tasks` (§8, mệnh lệnh 8).<br>**NỬA CÒN NỢ: chưa tác vụ nào được đăng ký trên máy thật.** `kiem-tra-lich.ps1` chạy trên máy dev 01/08/2026 in đúng `HubJobs : CHƯA ĐĂNG KÝ` và thoát `1` | Nửa khó (hàm, sổ ghi, trạng thái, chống chạy chồng, bộ bọc, script đăng ký, lệnh kiểm tra) đã xong và có test. Nửa còn lại **cố ý không do gói thi công làm**: đăng ký một tác vụ chạy bằng tài khoản SYSTEM là đổi cấu hình máy của người dùng — phải để họ tự bấm, sau khi đọc câu lệnh bằng mắt mình. Ngoài ra còn phải biết máy nào là máy chạy thật, mà chuyện đó gắn với quyết định triển khai VPS (ADR-019) | **Cổng go-live**, cùng mốc với nợ #19. Ba bước, đều đã có sẵn trong `tools/jobs/README.md` mục "Cắm lịch lên máy thật": (1) tạo file env NGOÀI kho + siết `icacls`/`chmod 600`; (2) chạy `dang-ky-lich` để xem, rồi chạy lại với `-XacNhan` bằng quyền quản trị; (3) `schtasks /Run` một lượt rồi `kiem-tra-lich`.<br>**Cảnh báo nghiệm thu (đo được 01/08/2026):** ĐỪNG lấy `ops.v_job_health` trên `hub_dev` làm bằng chứng — view báo `flag_engine` `state='ok'` lúc 13:05 "hôm nay" trong khi 313 dòng `job_runs` của hai ngày đó do **bộ test** sinh ra, không do lịch nào. Xem nợ #41.<br>**Cập nhật 02/08/2026 — mẫu số đã đổi, và đổi vì một tai nạn chứ không vì chủ ý:** trong lúc thử ngược cổng của nợ #41, một lệnh `drop database` chạy nhầm trên `hub_dev` (chi tiết ở `02-database.md`, mục "Database test tách khỏi hub_dev"). Server dựng lại được, nhưng **454 dòng `ops.job_runs` mất hẳn** nên hôm nay `hub_dev` có **0 dòng** lịch sử chạy job. Đây đúng là trạng thái sạch mà nghiệm thu #33 cần — nhưng nó tới bằng tai nạn, nên **không được kể như một thành tích**, và nghiệm thu tới phải chạy job thật rồi đọc số mới chứ không được đối chiếu với bất kỳ con số nào ghi trước ngày 02/08 | trước khi nạp dữ liệu thật |
| 34 | **Đợt rà đường đọc cột `mood` sau `0038` chưa xong.** `0038` thu quyền `SELECT` cột `mood` khỏi `authenticated` (ADR-025) và mở lại bằng view `attendance.checkins_care`; mọi câu đọc cũ còn `select *` hoặc `select c.mood` thẳng trên `attendance.checkins` dưới phiên người dùng nay nhận `permission denied for table checkins` | Đây là cái giá đã biết trước khi chọn GRANT-theo-cột thay vì view che cột (ADR-025): lỗi **ồn ào và dừng ngay**, không phải im lặng trả NULL. Thà vỡ ở test còn hơn một job đọc ra NULL rồi xoá sạch 12 tháng dữ liệu | Rà hết đường đọc rồi chạy lại HAI lệnh; xanh cả hai thì gạch dòng này. **Đo lúc 22:10 ngày 31/07/2026, khi các gói còn đang bay:** `tools/run-db-tests.sh` trên database dựng lại từ đầu → 517 assertion trên 39 file, **2 file đỏ** (`0017_checkins_self_update_test.sql`, `0023_principal_scope_test.sql`); `npx vitest run` → 12/35 file đỏ (10 ca), và **mọi lỗi DB đều cùng một câu** `permission denied for table checkins`. Ca đỏ còn lại không cùng họ: `tests/unit/nav-links.test.ts` bắt nhãn "Tâm lý cụm" dẫn hai nơi (sidebar mờ → `#`, tile → `/tam-ly`) — việc của gói `man-hinh-tam-ly-cum`.<br>**MỞ LẠI VÀ MỞ RỘNG 01/08/2026 (ADR-026, `0044`):** phạm vi `core.can_read_mood` vừa hẹp thêm một vai (GVCN bị cắt), nên đợt rà này chưa đóng được — nó có thêm một danh sách thứ hai, và danh sách này nguy hiểm hơn vì **không sinh ra lỗi 42501 nào cả, chỉ sinh ra số 0**. Đo thật trên database dựng lại từ đầu, đăng nhập cô Lan: bốn màn GVCN lấy `attendance.checkins_care` làm nguồn cho CẢ cột điểm danh chứ không riêng mood (`apps/hub/server/routers/care.ts` ~576 · ~623 · ~964 · ~1087 · ~1105 · ~1355). Cùng một câu LEFT JOIN — nguồn `checkins_care` trả 5/5 em `status NULL`, đổi nguồn về `attendance.checkins` trả 5/5 em `status=present`. Nếu không sửa cùng lượt thì bảng điểm danh của cô **trắng toàn NULL** và UI vẽ NULL thành "chưa điểm danh" cho cả lớp. Ba việc: (a) mọi cột không-phải-mood đổi nguồn về `attendance.checkins`; (b) `getDashboard` thôi tự tính cờ E_MOOD từ `checkins_care`, chuyển sang `care.flags` (chính là nợ #32); (c) `getReportApprovalQueue` đếm `checkin_days` từ `attendance.checkins`, còn ô "ngày tâm trạng Vui" của bản xem trước phía cô thì bỏ hẳn hoặc để `null` + in câu khác — **tuyệt đối không để rơi xuống 0** | ngay trong buổi |
| 35 | **C_CEFR là luật duy nhất trong 6 luật chưa có signal view.** `care.run_flag_engine()` khai tường minh mảng 5 luật đã cài và bỏ qua C_CEFR kèm lý do ghi vào metrics. **Rà lại 01/08/2026 — giữ nguyên nợ, có phép đo:** khai `ops.source_freshness('tutor_cefr')` lúc này KHÔNG đổi được một chữ nào trong `rules_skipped` (nhánh `c_implemented` của `0039` chặn trước nhánh `source_key`), chỉ thêm `tutor_cefr` vào `degraded_sources` mọi lượt chạy vĩnh viễn — tức hoàn tác đúng việc `0011` vừa làm ngày 31/07 | Chưa có nguồn dữ liệu lộ trình CEFR nào chảy vào Hub (connector Tutor/COR chưa nối): `tutor.cefr_results` và `tutor.cefr_trajectories` **0 dòng**, và `grep -rn "insert into tutor."` toàn repo ra đúng 2 hit, cả hai trong `0007_tutor_health_test.sql` và đều chèn `mastery_snapshots` — **không bộ ghi nào**. Chấm một luật bằng dữ liệu không tồn tại là đúng kiểu hỏng ADR-016 cấm.<br>**Ba câu hỏi thiết kế chưa ai chốt** (ghi ra để ngày connector ra đời không phải đoán lại): (1) **thứ tự bậc CEFR lấy ở đâu** — `level`/`expected_level` là `text` trần, không CHECK/enum/domain, và `select proname … where nspname='tutor'` trả 0 dòng: không có hàm so bậc nào trong DB, nên "dưới lộ trình" hiện chưa có nghĩa máy hiểu được; (2) **"2 kỳ" là 2 kỳ LIÊN TIẾP hay 2 kỳ bất kỳ** — ngưỡng `{"periods_below_trajectory": 2}` không nói; (3) **sắp thứ tự kỳ bằng cột nào** — `cefr_trajectories` chỉ có `period_code text`, không cột ngày nào, tự nó không sắp được. Thêm: tên `cefr_gap` ở mục Rev B của `04-flag-engine.md` chưa từng được định nghĩa (không công thức, không cột, không cửa sổ thời gian) | Dựng `care.v_signal_cefr` + khai `care.rules.source_key` cho nó **cùng lúc** với connector cấp dữ liệu — không dựng view trước connector, vì view rỗng đọc y hệt "không em nào lệch lộ trình". Chốt ba câu hỏi trên **trước** khi viết view, và chốt bằng dạng dữ liệu connector thật trả về chứ không bằng phỏng đoán.<br>**Đã trả một phần 01/08/2026 (`0043`):** lý do bỏ qua không còn chỉ nằm trong JSON — `ops.v_rule_health` hiện nó thành một dòng đọc được, và `needs_attention` **cố ý không bật** cho món nợ này để nó không thành đèn vàng sáng vĩnh viễn | khi connector CEFR ra đời |
| ~~36~~ | ~~**`02-database.md` và hồ sơ HTML chưa theo kịp `0039`–`0041`, và mục "Bộ quét cảnh báo" của hồ sơ HTML lệch version với `04-flag-engine.md`.**~~ **ĐÃ TRẢ 01/08/2026** — cả hai cổng xanh và có phép đo đứng sau: `node tools/check-sync.mjs` cổng 1 xanh cả 8 cặp (`04-flag-engine.md` v10 khớp mục `#canh-bao`), cổng 2 báo **`OK 148 đối tượng schema · từ 0023 trở đi đều được 02-database.md gọi tên`** — tức không còn tên nào phải ghi nợ, và `NO_TAI_LIEU` trong `tools/check-sync.mjs` vẫn **rỗng** (không đóng bằng cách miễn trừ). Lần gộp 01/08 kéo nốt `0045`+`0046` vào cùng cổng đó và đưa `sync-version` lên 24 ở cả hai phía. Trạng thái cũ, giữ lại làm hồ sơ: ~~`node tools/check-sync.mjs` đỏ ở CẢ HAI cổng lúc 22:15 ngày 31/07/2026: cổng 2 kể tên **15 đối tượng** migration đã tạo mà tài liệu chưa nhắc (`care.run_flag_engine`, cả cụm `report.*` của `0040`, cả cụm `ops.*` của `0041`); cổng 1 báo `04-flag-engine.md` (v8) ≠ hồ sơ người (v7)~~ | Đợt B chia việc theo file sở hữu để nhiều luồng chạy song song; hai file này thuộc gói khác và gói đó chưa hạ cánh. Cổng 2 đỏ là ĐÚNG VIỆC nó phải làm — nó tồn tại chính vì cổng 1 (so hai con số `sync-version`) là xanh giả khi không ai sờ vào tài liệu. **Cách chia việc đó vẫn được dùng lại ngày 01/08** (ba gói viết bản nháp vào `danh-cho-may/.wip/`, một lượt gộp duy nhất đưa vào hai file dùng chung rồi xoá thư mục nháp) — cái giá đã biết là cổng 2 đỏ trong lúc các gói còn bay, và cái được là không có xung đột nào | — | — |
| 37 | **Lý do bỏ qua luật nay được DỊCH Ở HAI NƠI, và hai nơi đó có thể trôi khỏi nhau.** Cùng ngày 01/08/2026 có hai gói hạ cánh song song: `0043` dựng `ops.v_rule_health` (dịch mã lý do sang câu tiếng Việt **trong SQL**, kèm ranh giới `needs_attention`), còn gói buồng lái GVCN đọc thẳng `ops.v_job_health.last_metrics -> 'rules_skipped'` rồi dịch **trong TypeScript** (`apps/hub/components/gvcn/scan-status.ts` ~dòng 106–109: `chua_cai_dat` → "chưa cài đặt"…). Hôm nay hai bản dịch khớp nhau; ngày `0039` thêm mã lý do thứ năm thì **chỉ một trong hai** được sửa | Hai gói chạy song song theo file sở hữu, không gói nào được chạm file của gói kia — đó là cái giá đã biết của cách chia việc này, và nó rẻ hơn cái giá của việc hai luồng cùng sửa một file. Cả hai bản dịch đều **fail-loud đúng hướng**: SQL trả nguyên mã lạ kèm câu "chưa có lời giải thích", TS rơi về `khong_ro` — không bên nào im lặng nuốt mã lạ | Hợp nhất về MỘT bản dịch khi gói màn Điều hành tới: cho `apps/hub` đọc `ops.v_rule_health` (đã có sẵn `giai_thich` + `needs_attention` + phân biệt `chua_chay`/`khong_ro`) thay vì tự parse JSON, và bỏ bảng dịch trong `scan-status.ts`. **Chưa ai đọc `ops.v_rule_health`**: `grep -rl "v_rule_health" apps/hub packages/core` = 0 file tính tới 01/08/2026 — màn `/dieu-hanh` vẫn chưa có dòng "Bộ quét 01/08 chấm 4/6 luật · bỏ qua C_CEFR (chưa cài đặt), C_MASTERY (chưa khai nguồn tươi)". Cùng lúc rà `apps/hub/server/routers/care.ts` chỗ `where source in ('attendance','evidence')` — danh sách cứng ấy đang là lớp phòng thủ thứ hai chống đèn vàng giả, bỏ nó phải bỏ cùng lúc với việc có màn hình đọc lý do | gói việc màn Điều hành kế tiếp |
| 38 | **`attendance.happy_days()` vẫn để lọt phép trừ hai khoảng ("differencing").** `0044` đã chặn câu hỏi một-ngày bằng lỗi `22023` (khoảng hẹp hơn 5 ngày bị từ chối) và đã cắt cổng xuống ba vai (`is_me ∨ is_my_child ∨ in_my_cluster`). Nhưng người ĐÃ qua cổng — phụ huynh, tâm lý cụm — vẫn có thể hỏi hai khoảng lệch nhau đúng một ngày rồi trừ, và suy ra ngày đó em có Vui hay không. Với tâm lý cụm thì vô hại (họ đọc thẳng được `checkins_care`); với **phụ huynh** thì đây là đúng ranh giới `0038`/ADR-025 đã chốt là họ không được vượt | Cách chặn kín duy nhất là ép khoảng hỏi về **biên tuần cố định** (chỉ nhận thứ Hai → Chủ nhật, hoặc nhận `week_start` thay vì cặp ngày). Làm vậy hôm nay thì gãy `report.buildGrowthReport` — nó hỏi đúng thứ Hai → thứ Sáu, một khoảng lẻ, và đó là khoảng nghiệp vụ đúng (tuần học 5 ngày). Sửa cho kín đòi đổi cả chữ ký hàm lẫn câu gọi ở `apps/hub/server/routers/report.ts`, tức chạm hai vùng sở hữu khác nhau. Chấp nhận tạm vì mặt tấn công đã hẹp hẳn: từ "một câu SQL cho ra nguyên nhật ký" xuống "phải chủ động trừ hai lần gọi, và chỉ người vốn đã đọc được báo cáo của chính con mình mới làm được" | Đổi chữ ký sang `attendance.happy_days(p_student, p_week_start)` trả số của đúng tuần đó, đồng thời sửa `buildGrowthReport` gọi theo. Kèm pgTAP: hai lần gọi lệch nhau một ngày phải cho cùng một kết quả (hoặc bị từ chối), không cho hiệu bằng 1. **Điều kiện trả là điều kiện kỹ thuật, không phải mốc thời gian:** ngay khi có một gói việc được cấp quyền chạm cả `packages/core/db/migrations` lẫn `apps/hub/server/routers/report.ts` | cùng gói với lần sửa `report.ts` kế tiếp |
| ~~39~~ | ~~**GVCN vẫn đọc được `care.flags.detail` đầy đủ nếu hỏi thẳng CSDL.** `0044`/ADR-026 cắt nhật ký cảm xúc khỏi tầm đọc của cô, và `apps/hub/server/routers/care.ts` cắt nốt tại tầng hợp đồng (`FlagDetail` là schema ĐÓNG ba khoá, có assertion soi nguyên chuỗi JSON đi ra client). Nhưng cột `care.flags.detail` thì KHÔNG bị che: RLS `flags_scope` gác bằng `core.can_see_care()` — hàm cố ý giữ nhánh chủ nhiệm để cô vẫn nhận cờ. Đo thật trên hub_dev 01/08/2026, phiên Cô Vân (…0008), `select f.detail from care.flags f where rule_code = 'E_MOOD'` trả về `{"mode": "streak", "nguong": 5, "negative_days": 6, "negative_streak": 6}` cho em Lê Đăng Khôi~~ | **ĐÃ TRẢ 02/08/2026** bằng `0049_chi_tiet_co_roi_tam_doc_cua_gvcn.sql` (gói `flags-detail-va-bo-chay-migration`). Chọn **hình thức (b)** — che cột, không bỏ số — sau khi đo: (a) làm **tâm lý cụm mất thật** (ADR-026 viết "tâm lý cụm giữ nguyên mọi quyền"; chuỗi 6 ngày khác hẳn chuỗi 15 ngày), và xoá luôn bằng chứng `nguong` mà ADR-023 cần cho lần rà ngưỡng sau ≥1 học kỳ. Thi hành: `revoke select on care.flags from authenticated` rồi `grant select (id, student_id, rule_code, as_of_date, origin, created_at)` — Postgres **không** cho revoke một cột ra khỏi quyền cấp ở mức bảng (chỉ WARNING), đúng đường `0025`/`0038`. Mở lại cho tâm lý cụm bằng view chủ-quyền `care.flags_tam_ly` (`security_barrier`, phạm vi `core.in_my_cluster` ⊂ `can_see_care` nên **không mở thêm dòng nào**) | **Đo lại dưới từng danh tính (hub_dev 02/08/2026):** cô Vân …0008 10 dòng cờ → vẫn 10, `select detail` → **42501**; cô Lan …0001 1 → 1, detail → 42501; cô Mai …0003 17 → 17, `care.flags_tam_ly` **17/17 có detail**; Minh …0005 và Hùng …0007 0 → 0. Bộ quét (`run-flag-engine --dry-run`) chạy y nguyên: 10 cờ mới, 3 luật. Buồng lái qua HTTP thật (cô Vân → `care.getDashboard`) HTTP 200, thẻ cờ E_URGENT hiện đủ. **Đường thứ tư đã rà:** `report.v_vaar_indicators` chỉ cấp cho `postgres` và không chạm `detail` — không gãy. Kiểm chứng: pgTAP `0049_chi_tiet_co_test.sql` **20 assertion** + `tests/db/flags-detail.test.ts` **8 ca**; thử ngược (`grant select on care.flags to authenticated`) → **7 assertion đỏ** trên hai file, hoàn nguyên → xanh. Một assertion của `0044` được **lật chứ không xoá** (nay là `throws_ok 42501`), phép kiểm hình dạng `detail` chuyển sang bài `0049` — plan của `0044` giữ nguyên 26 | — |
| 40 | **TRẢ MỘT NỬA 02/08/2026 (gói `kenh-bao-dong`, migration `0051`).** Nửa đã trả: `ops.outbox_messages` **nay có bộ gửi thật**. `tools/jobs/run-bao-dong.mjs` (khai trong `ops.job_schedule`, nhịp 1 giờ) sinh tin từ `ops.v_job_health` + `ops.v_rule_health` rồi gửi qua các kênh khai trong `ops.alert_channels`. Mỗi tin nay có **năm** trạng thái tách bạch — `cho_gui` · `da_gui` · `gui_hong` (kèm lý do nguyên văn) · `het_luot` · `khong_co_kenh` — và ràng buộc `check ((status = 'da_gui') = (sent_at is not null))` biến "bịa ra đã gửi" thành lỗi database. §9 là một **chỉ mục** chứ không phải một lời hứa: `alert_deliveries_mot_lan_idx` chặn ghi hai lần thành công cho cùng (tin, kênh). Hết lượt thử ⇒ `het_luot` ⇒ hiện ở `ops.v_bao_dong_ton` ⇒ vào `metrics.findings` ⇒ `ops.v_job_health.needs_attention` bật. **Nửa CÒN NỢ, và đừng đọc nhẹ đi:** kênh duy nhất có adapter thật hôm nay là một **tệp nhật ký** (`var/bao-dong/…`) — vẫn là kênh **KÉO**, vẫn phải có người đi mở ra xem, **không rung điện thoại ai lúc 2 giờ sáng**. Và bộ sinh báo động **nằm trong chính bộ lịch nó canh**: cron chết ⇒ không tin nào được sinh, kể cả tin "cron đã chết". **Nguyên văn món nợ gốc, giữ lại để đối chiếu:** **`ops.outbox_messages` có bộ GHI mà chưa có bộ GỬI — hệ biết job hỏng, nhưng không có đường nào tới một con người.** Đo 01/08/2026: bảng có đúng một chỗ ghi (`0039_flag_engine.sql:534`); `grep -rn "outbox" apps/ tools/ packages/core/src` = **0 hit**; `grep -rn "sent_at"` chỉ ra 2 hit, cả hai nằm trong chính `0008_staging_ops.sql`. Không tiến trình nào gửi bất cứ thứ gì đi. Kênh báo động duy nhất đang tồn tại là **mã thoát** của `run-all.mjs --check`, và nó **chưa có ai gọi**.<br>**SỬA MỘT CÂU SAI 01/08/2026 (khi gộp tài liệu).** Dòng này trước đó viết: *"nay đã có người gọi (tác vụ `HubJobs-SoiSucKhoe` mỗi sáng 07:30)"*. Câu đó **không đúng** và tự mâu thuẫn với nợ #33 ngay trong cùng file này. Đo lại: `schtasks /Query /FO CSV \| Select-String "Hub"` trên máy dev trả về **không dòng nào**. Thứ tồn tại là `tools/jobs/dang-ky-lich.ps1`, và script đó **chỉ IN ra** câu lệnh đăng ký trừ khi có `-XacNhan` — tức tác vụ 07:30 là một câu lệnh nằm trên màn hình, không phải một tác vụ đang chạy. Ghi lại lỗi này thay vì lặng lẽ xoá, vì nó đúng loại hỏng mà cả sổ nợ sinh ra để chống: **một việc mới làm được một nửa bị chép vào tài liệu như đã làm xong**. Kể cả khi tác vụ được đăng ký thật, nó cũng chỉ đổi cột "Last Run Result" của Task Scheduler thành `1` — **và không ai ngồi nhìn Task Scheduler** | Cái giá đã biết của việc chưa mua hạ tầng gửi tin (`10-mua-sam-ha-tang.md`): chưa có SMTP, chưa có Zalo OA. Đỡ được một phần: `ops.v_job_health` đã cho `needs_attention` một cột duy nhất để đọc, và `kiem-tra-lich` biến câu hỏi "đêm qua có chạy không" thành một lệnh chạy được. Nhưng cả hai đều là kênh KÉO (có người đi hỏi mới biết), không phải kênh ĐẨY | Hai việc, và việc (a) mới là việc thật: **(a)** mua kênh ĐẨY — Zalo OA hoặc SMS (`10-mua-sam-ha-tang.md`). Khi có: thêm `tools/alert/kenh/<tên>.mjs` xuất một hàm `gui({tin, kenh})` + một dòng `ops.alert_channels` bằng migration. **Không sửa bảng, không sửa hàm SQL, không sửa bộ gửi** — giao diện adapter dựng sẵn cho đúng ngày đó. **(b)** đăng ký người canh NGOÀI hệ (nợ #33): `run-all.mjs --check` đã trả mã thoát `1`, nhưng tác vụ `HubJobs` chưa có trên máy nào. Kiểm lại đã trả hết chưa bằng: `select * from ops.alert_channels where kind <> 'tep_nhat_ky' and enabled` phải có ít nhất một dòng, VÀ `schtasks /Query /FO CSV \| Select-String "Hub"` phải ra dòng | cùng mốc go-live với nợ #33 |
| ~~41~~ | ~~**Bộ test dùng chung `hub_dev` với sổ vận hành, nên nó BỊA ĐƯỢC lịch sử chạy máy.**~~ **ĐÃ TRẢ 02/08/2026 — bộ test có database riêng.** Lối ra: `hub_test`, dựng lại từ đúng bộ migration + đúng `seed.mjs`, và `DATABASE_URL` của mọi lượt test tự đổi tên sang nó (`tests/helpers/db-test-url.ts`, cắm vào `globalSetup` + `setupFiles` của `vitest.config.ts`). pgTAP cũng bị chặn: `tools/run-db-tests.sh` từ chối chạy nếu tên database không kết thúc bằng `_test`.<br>**Phép đo nghiệm thu:** trước khi tách, MỘT lượt `vitest run` đẩy `ops.job_runs` trên `hub_dev` từ 446 lên 451 (đo 00:05 ngày 02/08). Sau khi tách, một lượt `vitest run tests/db` trọn bộ: `ops.job_runs` trên `hub_dev` 0 → 0 dòng và `ops.job_runs_id_seq.last_value` 10 → 10 (KHÔNG nhúc nhích), trong khi cùng lượt đó sequence trên `hub_test` chạy 1 → 20. Rác test đã đổi nhà.<br>**Giá phải trả, đo được:** dựng nguội `hub_test` (50 migration + seed) mất 3,5 giây, MỘT lần; lượt sau chỉ tốn 80–90ms nhờ dấu vân (băm nội dung migrations + seed, ghi ở `test_meta.dau_van`). Bộ `tests/db` chạy 34–48 giây tuỳ tải máy — không đổi đáng kể so với trước. | Đây là điều kiện để `ops.v_job_health` và dải "Quét đêm qua" nói thật trên máy dev, và để nghiệm thu nợ #33 có bằng chứng dùng được | **Còn một mẩu chưa làm:** cột `ops.job_runs.mode` vẫn chưa có giá trị `test` (phương án trung hạn ghi trong bản cũ của dòng này). Không làm vì sau khi tách database thì nó không còn giải quyết vấn đề nào đang tồn tại — chỉ mở lại nếu có ngày nào đó bộ test buộc phải chạy chung database với sổ vận hành. | đã trả 02/08/2026 |
| 42 | **[ĐÃ ĐỔI BẢN CHẤT ở `0047` — món nợ này KHÔNG còn phải trả theo cách đã ghi]** Lời hứa gốc đã được thay bằng *"chưa bấm thì phần mềm chưa ghi tâm trạng của con"* (ADR-027 bản 2): tài khoản của em **không bao giờ** bị hạ về `pending` nữa, nên không còn việc "chuyển hàng loạt về pending" để mà nợ. Cái còn lại của món này là việc VẬN HÀNH: chạy chiến dịch xin phiếu để `core.v_consent_gap` về rỗng. **Nội dung cũ, giữ nguyên để đọc lại vì sao:** _Lời hứa "chưa bấm thì tài khoản của con chưa bật" mới đúng với tài khoản MỚI, chưa đúng với tài khoản ĐANG chạy._ `0046` dựng đủ cơ chế (status `pending`, `core.record_consent` bật/tắt trong cùng giao dịch) nhưng CỐ Ý không chuyển các tài khoản học sinh đang `active` về `pending`. Đo trên hub_dev 01/08/2026: `select count(*) from core.v_consent_gap` trả về **1** — đúng một em (Minh) đang có tài khoản bật mà chưa có phiếu; 108 em còn lại `core.students.user_id IS NULL` nên không nằm trong diện (**đo lại 01/08/2026 ở phiên nghiệm thu**: con số cũ ghi "63 em" là số đúng của lúc gói "màn điều khoản" đo, trước khi gói khối 7-8 gieo thêm 45 em trong cùng đợt — hai gói chạy song song nên số của gói này lạc hậu ngay trong ngày. Bài học đi kèm: số đếm trên `hub_dev` phải ghi kèm mốc đo, và con số đáng tin ở đây là `select count(*) from core.v_consent_gap` = **1**, không đổi qua cả hai lần đo, vì nó không phụ thuộc tổng số em) | Bật cổng trước khi phụ huynh có đường quay lại là tự đặt bẫy: hôm nay Zalo OAuth chưa nối, `/api/auth/dev-login` là DEV ONLY, và mã mời chết sau 15 phút kể từ lần đổi đầu (ADR-024). Chuyển hàng loạt về `pending` lúc này là khoá cửa của những đứa trẻ trước khi bố mẹ chúng có nút để bấm. Khoảng hở KHÔNG bị giấu: nó có tên (`core.v_consent_gap`), đếm được bằng một câu SELECT, và được nhắc ở CẢ HAI phía tài liệu — mục `0046` của `02-database.md` và mục "Hai việc phải xong trước khi áp cho toàn khối" trong `ho-so-he-thong.html` (gộp 01/08/2026; bản nháp `.wip/man-dieu-khoan.md` đã xoá sau khi gộp — để nó lại là dựng một nguồn sự thật thứ hai) | **Điều kiện kỹ thuật, không phải mốc thời gian:** ngày Zalo OAuth chạy thật (phụ huynh đăng nhập lại được bất cứ lúc nào). Lúc đó chạy backfill `update core.users set status='pending' ...` cho mọi em trong `core.v_consent_gap`, và **sau khi** đường ghi hộ ở điều #43 có nút bấm | ngay khi nối Zalo OAuth |
| 43 | **[THÔI LÀ ĐIỀU KIỆN CHẶN sau `0047`, vẫn phải làm]** Từ `0047` chính em vẫn tự bấm được "Mình cần gặp thầy cô" trong mọi trạng thái phiếu đồng ý, nên đường ghi hộ không còn là thứ chống đỡ cho một lỗ hổng — nó trở lại đúng vai trò của nó: thêm một đường cho em không muốn tự bấm. **Đường "thầy cô ghi hộ yêu cầu gặp" đã mở ở tầng dữ liệu nhưng chưa có NÚT BẤM.** `0046` thêm `attendance.help_requests.source='staff'` + `created_by` + policy `help_requests_insert_by_care`, có pgTAP chứng minh GVCN ghi hộ được. Nhưng buồng lái GVCN và màn tâm lý cụm chưa có ô nhập nào gọi đường đó — hôm nay muốn ghi hộ phải vào CSDL bằng tay | Đường ĐỌC thì đã đủ: buồng lái và màn tâm lý cụm đọc thẳng `attendance.help_requests`, nên một dòng `source='staff'` hiện ra ngay ở đúng chỗ tín hiệu khẩn vẫn hiện. Cái thiếu là chỗ NHẬP. Mở policy trước, nút sau là thứ tự đúng: policy là hàng rào, thiếu nó thì nút bấm cũng vô dụng | Một ô nhập trong buồng lái GVCN (và màn tâm lý cụm) gọi một procedure mới, **bắt buộc xong TRƯỚC khi** bật hàng loạt tài khoản `pending` ở #42 — nếu không thì đúng ca mà #42 và ADR-027 sinh ra để tránh sẽ xảy ra thật: em chưa được bố mẹ bấm nút, không đăng nhập được, và cũng không ai ghi hộ được | cùng đợt với #42 |
| 44 | **[TRẢ MỘT PHẦN ở `0047`]** Ca tệ nhất của món này — em bị 403 "Tài khoản này không phải học sinh" sau khi bố mẹ rút lại — **không còn tồn tại**: em không bị chặn nữa, và ở ca ô tâm trạng tắt thì `checkin.submitMood` trả `moodSaved=false` + `moodBlockedReason` để màn hình nói đúng một câu tiếng Việt cho em ("phần này đang tạm tắt, không phải lỗi của con; nút cần gặp thầy cô vẫn dùng được"). Phần còn nợ là các cửa KHÁC. **Người bị chặn chưa được NÓI LÝ DO ở đúng chỗ họ chạm vào.** Đo trên bản chạy thật 01/08/2026: sau khi phụ huynh rút lại đồng ý, cùng một cookie còn hạn gọi `/api/trpc/checkin.getAttendanceOverview` nhận `403 "Tài khoản này không phải học sinh."` — đúng về kỹ thuật (`getMyStudentId` không tìm ra dòng vì `core.current_user_id()` là NULL) nhưng sai về nội dung với một đứa trẻ. Ở cửa đăng nhập cũng vậy: `resolveIdentity` trả `null` cho mọi status khác `active` nên route không phân biệt được "không có tài khoản" với "đang chờ bố mẹ đồng ý" | Sửa đúng chỗ đòi chạm hai file ngoài phạm vi gói "màn điều khoản": `packages/core/auth-adapter/dev-provider.ts` (cho `resolveIdentity` trả lý do thay vì `null` trần) và `apps/hub/server/routers/checkin.ts`. Cả hai đang có gói việc khác giữ, và chạm vào lúc này là giẫm chân. Hôm nay chưa quan sát được ở trường thật vì học sinh chưa có đường đăng nhập nào ngoài DEV | Khi nối Google OAuth cho học sinh (cùng mốc với #42): lúc đó `resolveIdentity` trả về một mã lý do có tên, màn đăng nhập nói "tài khoản của em đang chờ bố mẹ đồng ý, em báo thầy cô chủ nhiệm giúp", và `checkin.*` trả cùng câu đó thay vì "không phải học sinh" | cùng đợt với #42 |
| 45 | **ĐÃ TRẢ 01/08/2026 (phiên nghiệm thu độc lập).** Người nghiệm thu sửa cả hai chỗ ngay trong phiên: `#40` → `#42` (hai lần) và `37 assertion` → `40 assertion`. Kiểm lại tại chỗ: chuỗi `` `DEBT.md` #40 `` không còn dòng nào trong ADR-027, và `select plan(40)` của `packages/core/db/tests/0046_dieu_khoan_test.sql` chạy ra đúng **40** dòng `ok` trên database dựng lại từ đầu — khớp con số ADR nay ghi. Sửa ở phiên nghiệm thu chứ không ở gói gộp là đúng chỗ: người nghiệm thu không sở hữu file nào nên không giẫm chân ai, và một lỗi tham chiếu để qua đêm là một lỗi người sau đi tìm nguyên nhân không tồn tại. **Nguyên văn món nợ, giữ lại để đối chiếu:** ADR-027 trong `ADR.md` trỏ sai hai chỗ, và trỏ sai về phía trấn an. Đo khi gộp tài liệu 01/08/2026: (a) ADR-027 dẫn `DEBT.md` #40 **hai lần** cho khoảng hở "tài khoản đang `active` chưa có phiếu" — món nợ đó thật ra là **#42**; #40 là chuyện `ops.outbox_messages` chưa có bộ gửi, không liên quan. (b) ADR-027 ghi phần kỹ thuật đã thi hành với "**37 assertion pgTAP**", trong khi `packages/core/db/tests/0046_dieu_khoan_test.sql` khai `select plan(40)` và chạy ra đúng **40** dòng `ok` trên database dựng lại từ đầu | Cả hai là lỗi tham chiếu chứ không phải lỗi quyết định — nội dung ADR-027 đúng với thứ `0046` thi hành, và người gộp đã đối chiếu từng đối tượng trong migration trước khi viết `02-database.md`. Không sửa tại chỗ vì `danh-cho-may/ADR.md` **nằm ngoài quyền sở hữu** của gói gộp (gói này chỉ giữ `02-database.md`, `ho-so-he-thong.html`, `.wip/**`, `DEBT.md`); sửa lén file của gói khác là đúng thứ mà cách chia-việc-theo-file sinh ra để chống. **Nhưng đừng coi nhẹ hướng của cái sai:** một ADR dẫn nhầm sang món nợ khác làm người đọc tưởng điều kiện mở cổng đã có người canh, còn một con số assertion thấp hơn thật thì lần đối chiếu sau sẽ thấy "thừa 3 assertion" và đi tìm nguyên nhân không tồn tại | Chủ sở hữu `ADR.md` sửa hai chỗ: `#40` → `#42` (hai lần), và `37 assertion` → `40 assertion`. Việc nhỏ, không cần phiên Hội đồng — nó là lỗi chính tả tham chiếu, không phải sửa quyết định. Kiểm lại bằng: tìm chuỗi `#40` trong dòng ADR-027 của `danh-cho-may/ADR.md` (phải không còn dòng nào), và so `select plan(N)` của `packages/core/db/tests/0046_dieu_khoan_test.sql` với con số ADR ghi | gói kế tiếp chạm `ADR.md` |
| ~~46~~ | ~~**Bộ pgTAP không có cổng nào so số assertion CHẠY ĐƯỢC với `select plan(N)`.**~~ **ĐÃ TRẢ 02/08/2026 — ba tín hiệu, cả ba đều có cổng.** (1) `not ok`; (2) `ran ≠ plan` so ngay trong lượt chạy, bắt đúng ca pgTAP chết giữa chừng mà KHÔNG in một dòng `not ok` nào; (3) **mốc từng file** ở `tools/pgtap-moc.tsv`, so bằng `tools/pgtap-plan-check.mjs`. Tín hiệu 3 là thứ hai tín hiệu kia không thể tự thấy: một bài xoá 3 assertion RỒI sửa `plan(40)`→`plan(37)` cho khớp thì lượt chạy hoàn toàn hợp lệ, và một file test bị đổi tên khỏi khuôn `*_test.sql` thì vòng lặp còn không biết mình vừa mất một bài.<br>**Mốc là BÁNH CÓC:** viết thêm assertion → mốc tự nâng và thành một diff trong PR; viết bớt → cổng đỏ, hạ mốc phải gõ `--cap-nhat` ra tay. Mốc ghi PLAN chứ không ghi số dòng `ok`, để nó đọc được cả trong một lượt chạy đang đỏ.<br>**Đã thử ngược, 02/08/2026:** (a) chèn `select khong_co_ham_nay_dau();` vào giữa `0002_core_identity_test.sql` → cổng in `chạy 4/8 assertion (lệch plan hoặc file dừng giữa chừng)` và gọi ĐÚNG tên file, dù output không có một dòng `not ok` nào; (b) đổi tên file đó khỏi khuôn `*_test.sql` → `0/48 file test có lỗi` nhưng cổng vẫn ĐỎ với `CÓ TRONG MỐC nhưng lượt này KHÔNG CHẠY — bài test biến mất (8 assertion)`; (c) hạ plan của `0039` 35→32 trong bảng kê → `plan TỤT 35 → 32 (mất 3 assertion)`, thoát 1. Đã hoàn nguyên cả ba. Mốc hiện tại: **832 assertion trên 49 file**, lượt chạy sạch khớp đúng. | Con số assertion in trong `02-database.md` và trong hồ sơ HTML từ nay là một khẳng định kiểm được, không phải một lần đo trong quá khứ | **Đã gộp 02/08/2026:** cả hai phía tài liệu nay in **832 assertion / 49 file** với `tools/pgtap-moc.tsv` là nguồn (`02-database.md` mục "Kiểm chứng của cả đợt F"; hồ sơ HTML mục Vận hành). Lượt gộp cũng bắt được một số sai trong chính bản nháp của đợt — bản nháp `0048` khai `plan(32)` trong khi file test khai **34**; đọc thẳng `select plan(N)` chứ đừng chép niềm tin từ bản nháp. Đã trả 02/08/2026 |
| 47 | **Lời hứa "một dòng nạp là một đơn vị" mới được thi hành và ĐO ở đúng nguồn `cor`.** `0048` bọc phần ánh xạ của `core.promote_cor_row()` vào một khối con nên dòng bị từ chối không còn ghi một nửa. Ba đường nạp còn lại chưa được đo bằng cùng phép đo đó: `core.promote_embedded_event()` (`0028`) và hai bộ đọc chưa viết của `staging.raw_moodle` / `staging.raw_tutor_events`. **Đọc mã `0028` thì thấy nó có vẻ không dính** — mỗi `return core.record_import_error(...)` ở đó hoặc nằm TRƯỚC lệnh ghi tương ứng (dòng 117 trước insert 124; dòng 146/150 trước insert 154), hoặc nằm trong `exception when others` của chính khối insert nên đã được hoàn tác. Nhưng "đọc mã thấy có vẻ" chính là thứ đã cho `0045` xanh 52 assertion: cái hỏng ở đó cũng vô hình khi đọc, chỉ hiện ra khi đọc một cột TRƯỚC lô rồi đọc lại SAU lô. Chưa có phép đo nào như vậy cho `0028`. Ngoài ra `0048` cố ý **không tạo tên schema mới** để cổng 2 của `check-sync` khỏi đỏ trong lúc gói khác giữ `02-database.md`; cái giá là toàn bộ phần ánh xạ nằm trong một hàm dài | Cái hỏng ĐÃ ĐO ĐƯỢC (`cor`) đã bịt, có test ở cả ba tầng và đã thử ngược cả ba. `moodle`/`tutor` chưa có bộ đọc nào chảy dữ liệu nên chưa gây thiệt hại cho ai; rà bây giờ là rà mã chưa ai chạy. Nguồn embed đang chạy nhưng chỉ ghi vào bảng sự kiện và `evidence.dear_logs`, không upsert vào thực thể lõi, nên phạm vi thiệt hại nếu có là hẹp hơn hẳn | (a) Rà `core.promote_embedded_event()` bằng ĐÚNG phép đo của `0048` (đọc một cột trước lô, nạp một lô bị từ chối, đọc lại) chứ không bằng đọc mã — việc này làm được ngay, không cần chờ ai; (b) mỗi bộ đọc nguồn mới phải có một bài test cùng hình dạng trước khi go-live; (c) tách phần ánh xạ của `promote_cor_row` thành hàm riêng và gọi tên nó trong tài liệu — **nửa tài liệu đã trả 02/08/2026** (mục `0048` đã có trong `02-database.md`, hai dòng bảng `0045` đã sửa theo, phần cho người đã vào hồ sơ HTML kèm sơ đồ View 07 vẽ lại thành **mũi tên hai nét**); nửa còn lại là việc tách hàm trong migration | (a) trong đợt kế tiếp; (b) ngay khi có connector nguồn mới |
| 48 | **Mức tâm trạng đã thu TRƯỚC khi phụ huynh rút lại vẫn nằm trong kho, và chưa có đường cho phụ huynh yêu cầu xoá sớm.** `0047` chặn việc GHI THÊM khi không có phiếu, nhưng cố ý không tự xoá dữ liệu cũ: rút lại đồng ý là "ngừng xử lý từ nay", còn xoá phần đã thu là một quyền KHÁC theo Luật 91/2025 và phải do người ta yêu cầu — tự xoá là tự quyết thay phụ huynh, và xoá một chiều thì không có đường lùi. Đếm được bằng `select * from core.v_mood_khong_phieu` (gọi tên từng em + số dòng + khoảng ngày) | Không giấu: hạn 12 tháng của `0031` vẫn chạy trên đúng các dòng này, nên món nợ có ngày hết hạn tự nhiên. Cái thiếu là đường NGƯỜI DÙNG yêu cầu xoá sớm — mà đường đó nằm chung gói với nút tự rút lại (chưa có) | Trả cùng lúc với nút tự rút lại trên ứng dụng: khi phụ huynh bấm rút lại, hỏi thêm một câu "xoá luôn phần đã ghi?" và ghi lựa chọn đó vào sổ đồng ý. Trước khi bật cổng cho toàn khối, chạy `core.v_mood_khong_phieu` một lần và báo cáo con số | mỗi tháng, và bắt buộc trước ngày go-live |
| 49 | **`seed.mjs` không tạo phiếu đồng ý cho em nào, nên trên `hub_dev` phần ghi tâm trạng đang TẮT với toàn bộ học sinh.** Đây là sự thật của trường hôm nay (chưa gửi phiếu tới phụ huynh nào) chứ không phải lỗi, và cố ý không giấu bằng cách nhét consent vào seed — giấu thì mọi phép đo về cổng đồng ý mất mẫu số. Hệ quả cụ thể cho người demo: đăng nhập vai Minh rồi bấm tâm trạng sẽ thấy màn "trường chưa ghi tâm trạng của con" (đúng thiết kế). Thêm nữa `DEV_ACCOUNTS` **chưa có phụ huynh**, nên không bấm đồng ý qua giao diện được — phải chạy SQL hoặc tự ký token | Bật demo bằng một câu: `insert into core.consent_records (user_id, student_id, terms_version_id, decision, content_hash) select p.user_id, ps.student_id, tv.id, 'granted', tv.content_hash from core.parent_students ps join core.parents p on p.id = ps.parent_id cross join (select id, content_hash from core.terms_versions where published_at is not null order by version desc limit 1) tv;` | Trả khi thêm một tài khoản phụ huynh vào `DEV_ACCOUNTS` (cùng gói với nợ #đã ghi ở mục dev-login của `02-database.md`), hoặc khi nối xong Zalo OAuth | trước buổi demo kế tiếp |
| 50 | **Tài liệu vừa nhận trách nhiệm mô tả ba thứ NẰM NGOÀI migration, mà cổng đồng bộ không soi được thứ nào.** Lượt gộp 02/08/2026 đưa vào `02-database.md` ba mô tả mới: hai kho khoá IndexedDB trên máy học sinh (`hub:queued-checkin:<clientId>`, `hub:checkin-chua-gui:<clientId>`), schema `test_meta.dau_van` chỉ tồn tại trong `hub_test`, và hợp đồng ba-đường-ra của `flushQueuedCheckins`. **Cổng 2 của `check-sync` chỉ đối chiếu bảng/view/hàm do migration TẠO RA** — nó không biết ba thứ này tồn tại. Đo được: `grep -rn "hub:checkin-chua-gui\|hub:queued-checkin" tests/ apps/hub` trả về đúng **2 hit, cả hai nằm trong chính `apps/hub/lib/offline-queue.ts`** — không một bài test hay cổng nào gọi tên hai khoá đó. Nghĩa là đổi tên khoá trong mã nguồn thì tài liệu sai ngay, và **mọi cổng vẫn xanh** | Ba mô tả này đáng có trong tài liệu hơn là không có: nợ #31 sống được nửa năm đúng vì kho trên máy học sinh chưa từng có một dòng nào trong hồ sơ. Cái giá đã biết của việc viết ra là tài liệu có thêm ba chỗ trôi được. Rủi ro hôm nay thấp: `tests/unit/offline-queue.test.ts` (10 bài) khoá HÀNH VI ba đường ra, nên phần dễ sai nhất — luật phân loại lỗi — vẫn có người canh; thứ không ai canh chỉ là TÊN KHOÁ và tên schema siêu dữ liệu test | Rẻ và làm được ngay: cho `tests/unit/offline-queue.test.ts` một assertion đọc `02-database.md` và đòi hai chuỗi khoá xuất hiện nguyên văn — cùng khuôn cổng quét nguồn mà `tests/unit/checkin-view.test.ts` đã dùng, và **phải thử ngược** (đổi tên khoá trong mã, cổng phải đỏ và gọi đúng tên khoá) trước khi tính là đã trả. Làm cùng lúc cho `test_meta.dau_van` trong `tests/helpers/chuan-bi-db-test.ts` | đợt kế tiếp chạm `apps/hub/lib/offline-queue.ts` |
| 51 | **`07-operations.md` vừa dài thêm một mục lớn (4d, năm kịch bản) mà `sync-version` KHÔNG tăng — luật đồng bộ bị bỏ qua một cách có chủ ý, và điều đó phải có tên.** Hai phía hiện cùng ở **v4** (`07-operations.md` và `<section id="van-hanh" data-pair="danh-cho-may/07-operations.md">` trong `ho-so-he-thong.html`), nên `check-sync` cổng 1 vẫn xanh — **xanh giả, đúng loại xanh giả mà cổng 2 sinh ra để bắt**. Lý do không tăng: tăng một phía làm cổng 1 ĐỎ, mà `ho-so-he-thong.html` nằm ngoài quyền sở hữu của gói này (gói này giữ `07-operations.md`, `so-tay-su-co.html`, `DEBT.md`); tăng cả hai phía là sửa file của gói khác. Chọn giữ cổng xanh + ghi nợ có tên, thay vì làm đỏ một cổng đang xanh | Có một lý do sâu hơn quyền sở hữu, và nó là lý do thật: **bản đối ứng cho người của mục 4d KHÔNG nằm trong `ho-so-he-thong.html`** mà nằm ở một file mới, `danh-cho-nguoi/so-tay-su-co.html`. Cơ chế `data-pair` chỉ biết đúng một file phía người, nên nó không có cách nào diễn đạt cặp này. Đây không phải người viết lười — đây là chỗ cổng đồng bộ chưa với tới | Hai việc, cùng lượt gộp kế tiếp: (a) tăng `sync-version` 4 → 5 ở **cả hai** phía và thêm vào mục "Vận hành" của hồ sơ một đoạn đọc-bằng-lời về năm kịch bản, trỏ sang `so-tay-su-co.html`; (b) cho `tools/check-sync.mjs` hiểu **bản đối ứng nhiều file** — ví dụ cho phép `ban-doi-ung` trong frontmatter kê nhiều đường dẫn và đối chiếu `data-sync-version` trên từng file. Không làm (b) thì mọi tài liệu cho người viết ra ngoài `ho-so-he-thong.html` từ nay đều là một vùng tối y hệt | lượt gộp tài liệu kế tiếp |
| 52 | **`danh-cho-nguoi/so-tay-su-co.html` không có cổng nào canh, và thứ nó chứa lại là những câu lệnh sẽ mục.** `tools/check-html.mjs` ghim cứng đúng một file (`const FILE = resolve(ROOT, "danh-cho-nguoi/ho-so-he-thong.html")`), nên sổ tay mới không được kiểm cân bằng thẻ, không kiểm id trùng, không kiểm ký tự cấm. Nguy hơn phần markup: sổ tay in ra giấy **16 khối lệnh** kèm câu trả lời mong đợi của từng khối. Ngày ai đó đổi tên container `pg_hub`, đổi cổng 5434, đổi tên cột `state`/`needs_attention` của `ops.v_job_health`, hay đổi tên `staging.v_loi_nap_danh_sach`, thì **tờ giấy dán trên tường sai ngay và không cổng nào đỏ** — mà tờ giấy đó được đọc đúng lúc người đọc không còn sức đi tra cái gì khác | Chấp nhận trong đợt này vì hai lẽ: bản thân việc CÓ sổ tay đã hơn hẳn không có (ô 21 checklist go-live nợ từ tuần 5), và mọi lệnh trong đó đã được gõ thật ngày 02/08/2026 nên hôm nay nó đúng. Cái thiếu là thứ giữ cho nó tiếp tục đúng. Rủi ro thật nhưng chậm: mấy cái tên đó ít khi đổi | Hai bước, bước (a) rẻ và làm được ngay: **(a)** cho `check-html.mjs` chạy trên MỘT DANH SÁCH file thay vì một hằng số, thêm `so-tay-su-co.html` vào danh sách đó (và các file `danh-cho-nguoi/*.html` khác đang mọc lên trong cùng đợt) — phần kiểm markup + ký tự cấm dùng lại nguyên xi, chỉ phần "đủ 19 mục / 8 data-pair" là riêng của hồ sơ. **(b)** một bài test đọc `so-tay-su-co.html`, rút các chuỗi định danh (`pg_hub`, `hub_dev`, `5434`, `ops.v_job_health`, `staging.v_loi_nap_danh_sach`, `run-flag-engine.mjs`, `start-local.sh`) và khẳng định từng chuỗi còn tồn tại trong kho / trong schema — cùng khuôn cổng quét nguồn của `tests/unit/checkin-view.test.ts`, và **phải thử ngược** (đổi một tên trong sổ tay, cổng phải đỏ và gọi đúng tên đó) trước khi tính là đã trả | (a) đợt kế tiếp chạm `tools/check-html.mjs`; (b) trước go-live |
| 53 | **Bộ dữ liệu mẫu không có cổng nào canh chuyện TRÙNG MÃ, và upsert biến va chạm thành ghi đè im lặng.** Đo thật 02/08/2026: thêm hai tài khoản học sinh, lấy nhầm hai UUID `40000000-…-000b/000c` đang thuộc về Cô Thu và Thầy Phúc (chủ nhiệm khối 7). `insert … on conflict (id) do update set full_name` KHÔNG cãi — nó đổi tên hai giáo viên thành tên hai em, nối hai em vào tài khoản giáo viên, và cấp thêm vai `student` cho họ. Seed vẫn in "OK — seed xong" và mọi test vẫn xanh. Lỗi chỉ lộ ra vì có người ngồi `select` đối chiếu bằng mắt | Đã dựng cổng CHẶN cục bộ trong `seed.mjs`: khối tài khoản học sinh mới `select email from core.users where id = $1` trước, lệch email là `throw`. Cổng này tự bắt được ngay lần đổi email kế tiếp trong cùng phiên — nó làm việc thật, không phải trang trí. Và phần in tài khoản cuối seed nay ĐỌC từ `dev-provider.ts` thay vì chép tay, nên hai chiều lệch (CSDL thiếu / danh sách thừa) đều tự báo | **Cổng mới chỉ canh ĐÚNG hai dòng học sinh đó.** Mọi khối insert khác trong `seed.mjs` (và cả `000_test_support.sql`) vẫn dùng `on conflict do update` trần, nên lần trùng mã tiếp theo ở bất kỳ khối nào khác sẽ lại im lặng y hệt. Việc đúng là một phép kiểm chung: bảng khai (mã → email chủ sở hữu) và một vòng kiểm trước mọi upsert người dùng | trước khi nạp bất kỳ danh sách người thật nào |
| 54 | **Middleware ôm một bộ đệm RIÊNG mà `xoaDem()` không với tới — CSP chậm hơn trang tối đa 10 giây.** `apps/hub/server/embed/registry-db.ts` và `apps/hub/middleware.ts` mỗi bên một bộ đệm; middleware chạy trên Edge nên không nhập được module kia. **Đo thật 02/08/2026 sau khi tắt một app:** trang `/embed/<app>` trả 404 **ngay lượt kế tiếp**, còn header `frame-src` vẫn kê tên miền cũ, và trở về `frame-src 'self'` ở lần đo sau đó — trong khung 10 giây của TTL. | Cổng THẬT là trang: nó 404 tức thì, nên không có iframe nào để mà nạp, và một allowlist trỏ vào trang không dựng iframe thì không cho phép điều gì. CSP ở đây là lớp phòng thủ thứ hai, không phải lớp thứ nhất | Hai điều phải biết trước khi dựa vào con số 10 giây: (a) nó là TTL, không phải một tín hiệu — không có đường nào ép middleware bỏ đệm sớm hơn; (b) ngày Hub chạy nhiều hơn một instance thì `xoaDem()` cũng chỉ xoá đệm của instance đang phục vụ request đó, nên **cả trang lẫn CSP** đều lệch tới 10 giây trên các instance kia — lúc đó phải chuyển sang tín hiệu chung (LISTEN/NOTIFY của Postgres là đường rẻ nhất, không thêm dịch vụ nào) | rà khi đổi mô hình triển khai |
| 55 | **Không có sổ nhật ký ai bật/tắt app nào lúc nào.** `core.embedded_apps` chỉ giữ `updated_by` + `updated_at` — tức trạng thái CUỐI CÙNG, không phải lịch sử. | Đủ để trả lời "ai sửa gần nhất", và đó là câu hỏi hay gặp nhất. Dựng một bảng sự kiện chỉ-thêm cho một bảng có vài dòng là xây trước khi biết mình cần gì | Ca không trả lời được: một app bị bật rồi tắt rồi bật lại trong cùng một ngày — sổ chỉ còn lần cuối. Nếu về sau có sự cố lộ dữ liệu qua app ngoài thì đây đúng là thứ người điều tra cần, và lúc đó nó đã mất | khi có app rổ Vàng thứ hai |
| 56 | **Hai cây trang chủ hiện HAI BỘ THẺ khác nhau cho cùng một em học sinh, và không ai biết đó là chủ ý hay là trôi.** Đo 02/08/2026 trên `home-view.tsx`: `MobileHome` dựng GrowthBanner + StreakCard; `DesktopHome` dựng GrowthBanner + ThisWeekCard + TodayCard. Em mở điện thoại thấy "chuỗi check-in" nhưng KHÔNG BAO GIỜ thấy "tuần này"; mở máy tính thì ngược lại. | Có thể đúng: điện thoại chật, và §1.5 đòi ít chữ. Cũng có thể chỉ là hai cây trôi xa nhau qua nhiều đợt sửa — mã không nói được vế nào. Chưa tự sửa vì **đổi thẻ nào hiện với em là quyết định sản phẩm**, không phải một lần dọn mã | Hai câu phải trả lời trước khi chạm vào: (a) danh sách thẻ của mỗi khổ màn có phải là một quyết định ĐÃ ĐƯỢC ĐƯA RA không, hay chỉ là hiện trạng · (b) nếu là quyết định thì nó nằm ở đâu — hôm nay không ở đâu cả. Cổng `tests/unit/hai-cay-trang-chu.test.ts` mới chỉ canh được ba khối đã gom vào `khoiChoVai()`; các thẻ trên KHÔNG nằm trong đó, nên chúng vẫn trôi được tiếp | khi chủ đầu tư chốt bộ thẻ cho từng khổ màn |
| 57 | **Một lượt chạy CHẬP CHỜN chưa tìm ra nguyên nhân — `tests/db/man-hinh-moi.test.ts`, ba phép của khối `checkin.getMyHelpRequests` cùng đỏ một lần rồi tự xanh lại.** Đo 02/08/2026 ~21h: lượt chạy đầy đủ trước đó 1006/1006 xanh · lượt kế tiếp (chỉ sửa TÀI LIỆU, không chạm một dòng mã nào) ba phép đỏ · hai lượt sau đó xanh trở lại, không sửa gì. Chạy riêng file đó: 21/21 xanh. | Hai giả thuyết đã LOẠI bằng phép đo, ghi ra để người sau khỏi đi lại: (a) *sót dữ liệu từ lượt chạy bị ngắt* — sai, file tự dọn ở `beforeEach` chứ không chỉ ở `afterAll`; (b) *dòng `help_requests` thừa trong kho* — sai, đếm được đúng 3 dòng và cả ba thuộc em khác (`…1304/1404/1504`, dữ liệu mẫu), không dòng nào của Minh | **Chưa có nguyên nhân, nên chưa có cách sửa.** Nghi vấn còn lại: máy chủ dev đang phục vụ CÙNG database `hub_dev` mà bộ test ghi vào — lượt đỏ xảy ra ngay sau một loạt lượt tải trang bằng trình duyệt. Nếu đúng thì cách chữa là bộ test TypeScript chạy trên database riêng (như pgTAP đã làm với `hub_test`), chứ không phải sửa bài test. Ai gặp lại: ghi ngay giờ, ghi máy chủ dev có đang chạy không, và ĐỪNG sửa assertion — assertion đang đúng | khi gặp lại lần thứ hai |
| 58 | **Ba điều khoản của DESIGN-GUIDELINES đang đá nhau với luật chung của công cụ rà thiết kế, và đợt rà 05/08/2026 chạm vào cả ba.** (a) §7 bắt thẻ cờ buồng lái dùng `border-left 5px` màu mức độ — công cụ xếp dải màu dày một cạnh vào diện cấm tuyệt đối; (b) §3 cho tile app chưa build `opacity:.45` — phủ mờ cả khối kéo nhãn chữ xuống 2,21:1, trong khi §11 nói tương phản KHÔNG có ngoại lệ; (c) §Motion chốt `popIn cubic-bezier(.34,1.56,.64,1)` — công cụ cấm easing nảy | Đợt này chỉ chạm vế NÀO ĐO ĐƯỢC BẰNG SỐ và không đụng quyết định thẩm mỹ: (b) đã sửa bằng cách dời mờ từ khối cha xuống ô icon — mắt vẫn thấy mờ, chữ phục hồi 12,55:1, không phá §3 về mặt tín hiệu. (a) và (c) GIỮ NGUYÊN: border-left ở buồng lái còn mang chức năng phân mức khẩn cho người lớn, và popIn là chuyển động đã duyệt trên bản giấy — bỏ chúng là quyết định của người duyệt thiết kế, không phải của một lượt rà | Chốt với người duyệt thiết kế: hoặc sửa §3/§7/§Motion cho khớp, hoặc ghi vào guideline rằng đây là ba ngoại lệ CÓ CHỦ Ý của trường. Trước khi chốt, KHÔNG ai được tự bỏ border-left ở buồng lái (student-detail-view đã tự bỏ một lần, nay hai màn cùng buồng lái nói hai ngôn ngữ hình — xem nợ 59) | trước go-live |
| 59 | **Hai màn cùng buồng lái phân mức khẩn bằng hai ngôn ngữ hình khác nhau.** `gvcn-dashboard.tsx` giữ `border-l-[5px]` theo §7; `gvcn/student-detail-view.tsx` đã bỏ (02/08/2026) và thay bằng nền nhạt + icon. Cùng một cô, cùng một buồng lái | Cả hai đều đọc được và đều không dùng màu làm tín hiệu duy nhất, nên đây là nợ NHẤT QUÁN chứ không phải nợ tiếp cận | Đi kèm nợ 58: khi người duyệt chốt một trong hai ngôn ngữ, sửa màn còn lại cho khớp | cùng lượt với 58 |
| 60 | **Cổng landmark/tiêu đề chỉ canh 6 file được liệt kê tay trong `tests/unit/a11y-nen.test.ts`.** Đợt 05/08/2026 kéo thêm checkin-view và home-view vào danh sách sau khi sửa; `growth-report-view.tsx` vẫn ĐỨNG NGOÀI vì nó dựng bằng hai loại vỏ (PageShell cho điện thoại, MainContent cho máy tính) nên phép đếm "số h1 bằng số vùng" đỏ dù cả hai nhánh đều đúng | Danh sách tay vẫn hơn không có cổng nào: nó đã bắt được đúng lỗi "vùng nội dung không chứa h1" ngay khi home-view được thêm vào | Dạy `mainRegions()` hiểu `<PageShell>` cũng là một vùng nội dung, rồi thêm nốt growth-report-view, login-form và các màn còn lại. Cách thử ngược bắt buộc: gỡ một `<h1>` ra, cổng phải đỏ và gọi đúng tên file | đợt kế tiếp chạm a11y-nen.test.ts |
| 54 | **Lượt chạy ĐẦU TIÊN sau khi thêm một migration làm đỏ vài file test không liên quan — đỏ giả, và đỏ ở chỗ khiến người ta đi tìm sai chỗ.** Đo thật 06/08/2026 ngay sau khi `0053` vào kho: `npx vitest run tests/db` trả **4 file đỏ** (`care-thresholds`, `kenh-bao-dong`, và hai file khác) với thông báo `function care.resolve_threshold(unknown, uuid) does not exist` — một hàm có từ `0026` và đang tồn tại trong CẢ `hub_dev` lẫn `hub_test`. Chạy riêng từng file: **xanh**. Chạy lại trọn bộ ngay sau đó: **28 file / 315 ca xanh**. Nguyên nhân: `tests/helpers/chuan-bi-db-test.ts` so "dấu vân" (bộ migration + seed) và **dựng lại `hub_test` từ đầu** khi vân lệch; migration mới làm vân lệch, nên lượt đầu tiên có một file đang dựng lại database trong khi các file khác đã nối vào và chạy — chúng thấy một database mới xoá xong, chưa dựng lại tới `0026` | Cơ chế dấu vân là thứ ĐÚNG và phải giữ: nó sinh ra để chặn ca "một `hub_test` cũ thiếu ba migration mới nhất trông y hệt một `hub_test` đúng". Cái thiếu chỉ là hàng rào chắn trong lúc dựng lại. Thiệt hại thật hôm nay bằng 0 (chạy lại là xanh), nhưng thiệt hại tiềm tàng thì không: người đọc thấy `resolve_threshold does not exist` sẽ đi đọc `0026` và router ngưỡng — hai chỗ hoàn toàn vô can | Dựng lại phải diễn ra MỘT LẦN, TRƯỚC khi worker nào chạy: chuyển việc so vân + dựng lại vào `tests/global-setup-db.ts` (đã có sẵn trong `vitest.config`), hoặc giữ ở chỗ cũ nhưng thêm khoá liên tiến trình (`pg_advisory_lock` trên chính `hub_test`) để file nào tới sau thì CHỜ chứ không chạy vào giữa lượt dựng. Nghiệm thu bắt buộc là **thử ngược**: thêm một migration rỗng để vân lệch, chạy trọn bộ `tests/db` **ở lượt đầu tiên** — phải xanh, không phải "chạy lại thì xanh" | ngay đợt kế tiếp chạm tests/helpers |
| 55 | **Đường ghi `absent` cho một dòng gửi muộn mới chỉ được canh bằng QUY ƯỚC, chưa bằng cưỡng chế — và cái comment canh nó đã sai suốt bốn ngày mà không ai bắt.** Đo 06/08/2026 dưới danh tính một GVCN thật, trên một dòng `queued_late` thật: một câu `update attendance.checkins set status='absent'` trả **`UPDATE 1`**, sổ `attendance.late_decisions` **0 dòng**, không lý do nào phải nhập. Nguyên nhân: `0032` thêm policy `checkins_update_by_homeroom` với `with check (… status in ('present','late','absent','excused'))`; policy PERMISSIVE thì Postgres **OR** chúng lại, nên vế hẹp của `checkins_confirm_late` (`0014`) không còn chặn được gì. Hệ quả kép: (a) `comment on policy` của `0014` — câu *"không tự ý sang absent"* — **sai từ ngày `0032` vào kho**, và một comment sai còn tệ hơn không có comment vì nó làm người đọc thôi kiểm; (b) ADR-029 tưởng mình MỞ một quyền, thật ra chỉ gắn GIÁ (lý do + sổ) cho quyền đã mở | Hàng rào thật sự bảo vệ dữ liệu vẫn còn nguyên và đã đo: chỉ GVCN **của chính lớp đó** mới update được, học sinh và GVCN lớp khác đều 0 dòng; `reporting` không có quyền; sổ không cho update/delete. Cái hở là **đường đi vòng cho đúng người**, không phải cửa mở cho người ngoài. Và đường vòng đó hôm nay không có UI nào dẫn tới — muốn đi phải mở psql | Ba đường, phải chọn một và ghi thành ADR chứ đừng vá lặng: **(a)** siết `checkins_update_by_homeroom` thành RESTRICTIVE hoặc thu hẹp `with check` — rẻ nhất nhưng đụng màn "Điểm danh lớp" (sửa điểm danh ghi nhầm là việc hằng ngày của GVCN, không nên bắt nhập lý do); **(b)** trigger trên `attendance.checkins` chặn mọi lượt rời `queued_late` mà không kèm một dòng `late_decisions` trong cùng transaction — cưỡng chế đúng chỗ, giá là một trigger nữa trên bảng nóng nhất; **(c)** chấp nhận có chủ ý và ghi rõ trong `02-database.md` rằng đây là quy ước. **Nghiệm thu bắt buộc dù chọn đường nào:** chạy lại đúng phép đo ở trên (UPDATE thẳng dưới danh tính GVCN) và đòi nó **thất bại**, chứ không đọc mã rồi kết luận | trước khi nạp dữ liệu thật (cùng mốc #19) |
| 61 | **ĐÃ CẮM MÁY ĐO 07/08/2026 — `scope` per-client vừa được cưỡng chế lần đầu, và trước hôm nay không ai đo được RP thật đang xin gì.** ADR-032 nối `sso_scopes` xuống `oidc-provider` — trước đó trường này được khai rồi không dùng, nên mọi RP xin được cả bốn scope. Đo trên `hub_dev` 07/08/2026: `ops.audit_log` có **3** dòng `oidc_token_issued` cho `factory`, và cột `scope` của cả ba **rỗng** — vì audit ghi ở `grant.success` (endpoint token), nơi `params.scope` thường vắng mặt. Nghĩa là hệ **không có dữ liệu** để trả lời "RP nào đang xin scope nào" | Rủi ro có trần và có đường lùi rẻ: Factory khai `{openid, profile}` trong `clients.ts` từ 29/07 và migration `0055` chép đúng bốn dòng đó; nếu gãy thì gãy **to và ngay** (`invalid_scope` tại `/oidc/auth`), không phải hỏng câm. Chữa là **tích thêm một ô** trên `/quan-tri/mini-app`, có hiệu lực ≤10 giây, không deploy | **ĐÃ LÀM:** `provider.on("authorization.success")` ghi `ops.audit_log` action `oidc_scope_requested` kèm nguyên văn `ctx.oidc.params.scope` — đó là chỗ scope thật sự tồn tại, khác `grant.success` nơi nó thường vắng mặt. Nghiệm thu bằng cách chạy TRỌN luồng đăng nhập Factory qua đường hầm, không đọc mã rồi kết luận: dòng đầu tiên vào sổ là `oidc_scope_requested · factory · "openid profile"`, khớp đúng `sso_scopes` trong sổ đăng ký. **Việc còn lại là ĐỌC nó** — sau một tuần chạy `select object_id, scope->>'scope', count(*) from ops.audit_log where action = 'oidc_scope_requested' group by 1,2` rồi đối chiếu từng app: lệch chỗ nào thì hoặc RP xin thừa, hoặc sổ khai thiếu | trước khi cắm RP thứ ba |
| 62 | **Đệm RP OIDC 10 giây là trần thật khi chạy nhiều instance — và nó nằm trên đường thu hồi.** `apps/hub/server/oidc/clients.ts` giữ danh sách RP trong RAM tiến trình; `xoaDem()` (gọi từ `routers/admin.ts` sau mọi mutation) chỉ xoá đệm **của chính tiến trình đó**. Hôm nay Hub chạy MỘT deployable (ADR-001/018) nên đệm tiến trình = đệm toàn hệ và thu hồi có hiệu lực tức thì. Ngày chạy >1 instance thì instance không nhận lệnh vẫn cấp token cho một app vừa bị tắt, tối đa 10 giây | Cùng họ với #22 (rate limit) và #54 (đệm middleware), và trần 10 giây là có thật chứ không phải vô hạn. Bảng `core.embedded_apps` vẫn là nguồn duy nhất — không có bản sao nào để lệch, chỉ có độ trễ | Chọn một: (a) `LISTEN/NOTIFY` trên kênh `so_dang_ky_doi` — Postgres đã có sẵn, mỗi instance tự xoá đệm khi nhận tin; (b) hạ đệm xuống 0 cho `napOidcClients()` và trả giá bằng một truy vấn mỗi lượt `/oidc/token`; (c) đệm chung ngoài tiến trình. **Nghiệm thu:** dựng hai instance, tắt app ở instance A, đòi instance B từ chối `/oidc/auth` **ngay lượt kế tiếp** | cùng mốc #22 — ngày quyết định chạy nhiều instance |
| 63 | **MỞ LẠI CÓ CHỦ Ý 08/08/2026 — chủ đầu tư quyết: *"bỏ mã mở khoá đi, ko cần"*.** Cửa đăng nhập thử trên `hub.truongvietanh.com` **không đòi mã**: `POST /api/auth/dev-login` với `authUid` của tài khoản quản trị, không kèm gì cả, trả **200 + cookie phiên** (đo lại sau khi gỡ, từ Internet). Ai biết địa chỉ đều vào được bằng một vai bất kỳ.<br>*Lịch sử để không ai đọc nhầm đây là quên:* nợ này từng được TRẢ 07/08/2026 sau khi đo ra rằng hàng rào `DEV_LOGIN_SECRET` đã xây từ 02/08 nhưng **chưa từng được bật** — cảnh báo bước 2c của `tools/start-local.sh` in ra mỗi lượt khởi động suốt năm ngày và bị đọc lướt qua nhiều lần, kể cả bởi chính tôi khi báo cáo. Đặt mã xong, đo lại: không mã → 401. Hôm sau chủ đầu tư bỏ mã. | Cùng lý lẽ đã ghi trong `dev-gate.ts` từ 02/08: một mật khẩu dùng chung mà **quy trình bình thường tự in nó ra** thì nó không bảo vệ ai, chỉ làm phiền đúng người có quyền — chuỗi đó đã rò hai lần trong một ngày, cả hai lần đều do công cụ tự in. **Hàng rào thật không phải mật khẩu mà là `DEV_LOGIN_CHO_PHEP_O_BAN_THAT`**: ở `NODE_ENV=production` cửa chỉ tồn tại khi có biến đó, mà nó nằm trong `apps/hub/.env.local` — file KHÔNG lên GitHub và KHÔNG đi theo lên máy chủ thật của trường. **Điều kiện đo lại 08/08/2026:** sau cửa có **109 học sinh · 17 tài khoản, tất cả là dữ liệu mẫu do seed sinh ra** (`Trần Hải My`, `Bùi Tiến Dũng`… — không phải người thật). | **Việc phải làm, và mốc để làm:** ngày nạp danh sách học sinh THẬT — hoặc ngày mở cửa cho người ngoài đội thử — thì **khoá lại trước khi nạp**, không phải sau. Một dòng `DEV_LOGIN_SECRET=<≥12 ký tự>` vào `.env.local` rồi khởi động lại; không sửa một dòng mã nào. Nghiệm thu bằng đúng phép đo ở cột đầu: `POST /api/auth/dev-login` từ Internet phải trả **401 và 0 cookie**. Cổng CI đòi bước 2c xanh vẫn còn nợ — nhưng nay nó phải kiểm được **cả hai chiều** (đang mở là ĐÚNG với hôm nay), nên nó là một cổng đọc cấu hình chứ không phải một cổng chặn | khi nạp dữ liệu thật (cùng mốc #19) |
| 64 | **ĐÃ TRẢ 07/08/2026 — hàng rào chống dò mã canh MỘT trong HAI cửa.** `/api/auth/dev-gate` giới hạn 5 lượt/phút/IP. `/api/auth/dev-login` thì không — mà nó cũng nhận đúng bí mật ấy qua header `x-hub-dev-secret` và trả 200 + cookie phiên khi đúng. Đo thật trên bản đang chạy: 8 lượt sai vào `/dev-gate` → `401×5` rồi `429`; 7 lượt sai vào `/dev-login` → **401 cả bảy**. Đi vòng qua hàng rào chỉ tốn việc đổi đường dẫn. | Mức nguy hiểm thật có trần: mã 20 ký tự trên bảng 56 chữ ≈ 116 bit, dò không tới. Nhưng một hằng số `DEV_GATE_ATTEMPTS_PER_MINUTE = 5` được khai rồi chỉ canh nửa số cửa là đúng loại lỗi kho này tồn tại để chặn — và nó chỉ lộ ra vì có người đo cửa thứ hai | **Đã vá:** `/dev-login` đếm cùng khoá `dev-gate:<ip>`, chỉ trừ hạn mức khi người gọi CÓ đưa mã và mã sai. Hai file dùng chung `lib/client-ip.ts` — ngân sách chung chỉ đúng khi hai bên tính ra cùng một chuỗi IP. Đo lại: 8 lượt sai vào `/dev-login` → `401×5` rồi `429`; và 3 lượt ở cửa này + 3 lượt ở cửa kia tiêu chung một xô. `tests/unit/dev-login-gate.test.ts` +2 phép canh | — đã đóng |
